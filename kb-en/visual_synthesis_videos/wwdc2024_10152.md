### What the images show
Basis: 12 of 12 frame sheets viewed, all codes checked.

- The content slide has fixed anatomy: large title at the top, bullet list on the left and the presenter on the right inside a rounded-corner card, with the bullets entering one by one between frames instead of appearing all together (sheet 0001, q0005 to q0007). The same slide carries a circular badge with the version in the top right corner and a credit footer citing the previous session about windowed apps (sheet 0001, q0007).
- An agenda list with two to four stacked topics, without numbering, marks the session's progress only through the weight and color of the text, current item in bold black and the rest in light gray. The change happens between frames, with one item leaving bold and the next one entering (sheets 0002 q0017 to q0018, 0003 q0019, 0008 q0065 and 0009 q0081).
- The pair of states that the API describes is shown as a disassembled component before it turns into code: a gray card shaped like a capsule with a circular avatar on the left, where the inactive state has a smaller, solitary avatar, the active one has a larger avatar with lateral indent, and the expansion reveals name and subtitle. An intermediate frame shows the capsule already wide but empty, that is, the expanded shape without the text (sheet 0003, q0023 to q0026).
- The video's standard screen is code on the left and the Vision Pro simulator on the right, a window with a bar of three colored dots and rounded corners running a video app with poster, synopsis, side navigation and a highlights strip. The profile button inside it physically demonstrates the effect that the code next to it just declared (sheets 0004 to 0007, 0011 and 0012).
- Every code block carries a section comment at the top naming the technique in focus, among them scale, clipping, opacity, delay, reusable effects and cross fade, and the newly added snippet appears marked with a light blue selection background. This highlight works as a guide to what changed since the previous frame and is not mentioned in the speech (sheets 0004 to 0011).
- The construction of the scale effect is visible line by line: first the button style with only highlight and capsule shape, then the hoverEffect call with a five percent scale in the active state, and finally the state parameter highlighted in blue, with the app running alongside the whole time (sheet 0004, q0033 to q0036).
- The clipping effect is assembled parameter by parameter: the clipShape call gets width and height calculated from the geometry proxy, then receives the anchor at the leading edge, and in the following frames the highlight moves from one argument to another, indicating progressive editing (sheet 0005, q0040 to q0044).
- The synchronization problem is demonstrated visually before the solution: the code isolates the opacity on the text component inside a horizontal stack, without touching the icon, and the slides show the capsule with the text absent and then present, which is exactly the flaw described (sheet 0006, q0048 and q0051 to q0053).
- Grouping appears in both forms, one after the other in the same code: the explicit version with namespace, a declared group and the group passed along in each effect call, and the implicit version with a single group modifier applied at the end of the parent view's chain (sheet 0007, q0058 to q0063).
- The delay is asymmetric and selective, and the image shows this better than the description: the animation wraps the clipping with a longer delay when activating and a shorter one when deactivating, while the scale line appears highlighted separately, outside the animation block (sheet 0009, q0073 to q0074).
- The slide about animations uses code terms in small light gray background boxes in monospaced font, in the middle of the bullets' running text, and the list grows from one to three items between frames, closing with the restriction on the unsupported animation type (sheet 0009, q0078 to q0080).
- The accessibility alternative is built as a type swap, not as a loose conditional branch: the effect becomes a reusable type conforming to the protocol, the reduce motion preference comes in through the environment and a ternary operator chooses between an empty effect and an expansion effect, with the green badge marking something new next to it. Then the button's background becomes a stack with two separate shapes, wide capsule and circle, each with its own fade, and the highlight alternates from one to the other (sheets 0010 q0085 and q0086, 0011 q0094 to q0099).
- The closing slide swaps vector illustration for photography: simple handcrafted objects on a wooden table on the right, with three recommendations entering one by one on the left, start simple, include anchoring elements and test thoroughly (sheet 0012, q0103 to q0105).
- The editing uses purely visual resources between blocks: empty white frames, ghost images of the faded previous code, an isolated green circle at the center and, at the end, a translucent blue layer over the presenter with the event mark as a watermark (sheets 0005 q0044, 0007 q0057 and q0062, 0010 q0088, 0011 q0093 and 0012 q0107).
- The green circular badge marking something new appears at specific points and not decoratively, marking the slide that introduces the custom effects API and, later, the empty effect used in the accessibility swap (sheets 0002 q0014 and 0011 q0095).

Visual proportion: by the notes, almost the entire video alternates between bullet slides and code with the simulator alongside, and the presenter appears interspersed in practically every sheet without occupying long blocks alone.
Recorded divergences or limits: the notes correct a reading within the same sheet, noting that three frames of the block are not the simulator but the slide version of the same card (sheet 0006, q0052 to q0054). There is also a frame where the group modifier's parameter appears empty with a highlighted space, interpreted as an edit in progress and not as final code (sheet 0008, q0064), and two pairs of frames with no visible change, just a camera cut (sheet 0006 q0046 to q0047, sheet 0009 q0078 to q0079).
