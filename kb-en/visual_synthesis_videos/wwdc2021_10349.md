### What the images show
Basis: 8 of 8 frame sheets viewed, all codes checked.

- The entire session runs on a single composition pair: code on the left in monospaced font with colored syntax, rendered result on the right, over a dark background, used from the first example to the final table (sheets 0001 to 0007; first pair at q0008 and q0009, with the outlined symbol generated by the plain image and the same symbol followed by the title when generated by the component with a label). The notes record that this visual structure is not stated in the speech.
- Accessibility is explained by swapping the symbol's drawing for the text in quotes that would be read aloud, in the same right-hand column: first the generic reading, then the custom symbol's technical name, then descriptive captions when the accessibility modifier is added, and finally the localizable strings file line that resolves the custom symbol (sheet 0002, q0014 to q0018).
- The same label component is rendered in four different button styles on the same screen, one bordered, one selection-field type, one capsule and one dark, to show adaptation to context without changing the code (sheet 0002, q0010).
- The comparisons isolate one parameter per row in table form: four color variations of the same label, three font sizes that shrink the symbol and the text together, and three image scales that change only the symbol's size relative to the text (sheet 0003, q0021 to q0025).
- The variant propagation by hierarchy is proven between frames: the list code gains the filled variant line with a new-feature badge and, in the same step, the list icons inside the simulated iPhone frame go from outlined to filled (sheet 0004, q0032 to q0033).
- A table crosses the symbol's shape variants (line, circle, square, rectangle) with the fill, isolating one variant per row and showing the standard outlined version next to the filled version, first for a system symbol and then for the custom symbol (sheet 0004, q0035 and q0036).
- The four rendering modes are presented as a four-row table with the same six example icons in each one, colored differently per row; the same table returns at the closing, alternating the left column between the mode's name and the corresponding modifier's code (sheet 0005, q0037; sheet 0007, q0058 to q0063).
- The mode change is demonstrated inside a simulated macOS window, with the title bar of three colored buttons serving as the platform frame: when the multicolor rendering line is added, the list icons stop being all blue and start having color by suit (sheet 0005, q0039 to q0042).
- The palette mode is explained by mapping up to three colors to three named categories next to the icon, with the category dimming when the symbol does not have that layer; two icons side by side compare a two-layer variant with a three-layer one, and then the code drops to two colors and two categories fade out (sheet 0006, q0049 to q0053).
- Material appears as a legitimate color style: in the row of three buttons, the third one shows frosted glass blurring the background behind the arrow, an effect that exists only in the image, since the speech names the modifier without describing the appearance (sheet 0007, q0055 and q0056).
- The system color palette enters as a large side legend, each parameter name accompanied by its color swatch, including the four semantic levels from primary to quaternary (sheet 0007, q0063).
- The video's annotation language is constant: a gray box surrounds the line of code that was just added and a green new-feature badge marks the feature of the year, appearing in the highlighted variant suffix in the tab bar code, in the list's variant line, in the hierarchical mode line and in the foreground styles line (sheet 0003, q0027; sheet 0004, q0028 and q0033; sheet 0005, q0045; sheet 0006, q0049).
- The symbol embedded in running text via interpolation appears rendered at the end of a sentence listing people, showing the glyph flowing together with the text (sheet 0003, q0019 and q0020); right below, a real tab bar with four icons and the active tab in blue closes the component context example (q0026 and q0027).
- The closing uses a simple bullet-point list in dark text on a light background, overlaid on the semitransparent image of the presenter, with no icons, followed by the black screen with the event's mark (sheet 0008, q0065 and q0069).

Visual proportion: almost every screen frame is code with rendered result, comparative table or device and window frame; the presenter in the studio appears interspersed in almost every sheet, with no studio frames recorded in sheet 0003, and the notes do not record any live demonstration inside a real app, only simulations next to the code.

Recorded divergences or limits: in sheet 0005 the notes point out that the hierarchical mode line is added to the code with a new-feature badge, but the two icons next to it remain visually the same, in the same color, with no perceptible difference in the frame. In sheet 0004 the last two columns of the custom symbol table show the name written out instead of the drawn symbol. Several pairs of frames were recorded as identical to each other, with no visible difference, in sheets 0001, 0003, 0004, 0005, 0006, 0007 and 0008.
