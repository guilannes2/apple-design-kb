# HIG, página lists-and-tables: o que as ilustrações e vídeos mostram

Status visual:
- hig-img_lists-and-tables: assistido: 1 de 1 folhas abertas, todos os códigos conferidos

# hig-img_lists-and-tables, ilustrações da página lists-and-tables

### hig-img_lists-and-tables · folha 0001 · código 9C7C3
- img 0697 (clara), seção Lists and tables: diferente do padrão de esboço com grade de guias das outras aberturas de seção, aqui a ilustração mostra diretamente uma tabela agrupada estilizada em tons de vermelho/laranja: título "Grouped Table View Header" acima de um cartão com 3 linhas de texto ("Table Row 1", "Table Row 2", "Table Row 3"), cada uma com um chevron ">" à direita e uma linha divisória fina entre as linhas, e "Grouped Table View Footer" abaixo do cartão. Confirma a descrição oficial ("stylized representation of a three-row table with header and footer text, tinted red").
- img 0698 (clara), seção iOS, iPadOS, visionOS: lista agrupada (cartão cinza claro com cantos arredondados contendo um cartão branco interno) com 3 linhas, cada uma com o texto genérico "Title" à esquerda e um ícone circular de "i" (informação) em azul à direita, separadas por linhas divisórias finas. Confirma a descrição oficial: o botão de informação aparece na borda final (trailing) de cada linha.
- img 0699 (clara), seção iOS, iPadOS, visionOS: mesma estrutura de lista agrupada, 3 linhas com "Title" em negrito à esquerda, mas agora cada linha também mostra um texto secundário cinza "Detail" antes do chevron ">" que aponta para a direita. A descrição oficial menciona apenas o chevron na borda final de cada linha, sem citar o texto "Detail"; a imagem acrescenta essa coluna de valor secundário que a descrição oficial não descreve.

## Síntese visual da página lists-and-tables
- A ilustração de abertura (img 0697) foge do padrão de "esboço com grade de guias sobreposta" visto nas aberturas de outras páginas (teclado, pessoas, ícone de compra, layout, launching): em vez de um desenho geométrico anotado, mostra diretamente uma tabela de exemplo com cabeçalho, três linhas e rodapé, na paleta de cor da seção.
- A comparação lado a lado de img 0698 e img 0699 ilustra visualmente a distinção central do texto da página: o botão de informação (círculo com "i" azul) é usado para revelar detalhes sem navegar, enquanto o chevron ">" (aqui acompanhado de um texto "Detail" que a descrição oficial não menciona) indica navegação para o próximo nível de hierarquia.
- As três imagens usam a mesma estrutura de lista agrupada em cartão com cantos arredondados e linhas divisórias finas, mudando apenas a cor de fundo (vermelho na abertura, cinza claro nas duas ilustrações de comparação) e o elemento à direita de cada linha (chevron simples, ícone de informação, ou chevron com texto secundário).
