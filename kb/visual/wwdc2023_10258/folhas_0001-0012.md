### wwdc2023_10258 · folha 0001 · de 00:00 a 01:10 · código 11A43
- Tela: abertura com logo da Apple estilizado em preto com reflexo colorido (q0001), seguida de forma de anel abstrato multicolorido com texto parcial ao fundo (q0002). Da metade em diante, o apresentador (homem, camisa jeans cinza) fala em pé, apoiado numa mesa de madeira clara, com um iPad Pro numa capa teclado escura e dois cubos de brinquedo (azul e vermelho) sobre a mesa; ao fundo, parede ripada de madeira e uma parede branca lisa. Em um quadro (q0006), a cena muda para um painel claro com quatro ícones azuis em linha: um celular com ondas de sinal, um tablet com um ícone de pessoa, uma forma de onda sonora vertical e um sol.
- Construção de interface: paleta azul consistente para os ícones de sistema (celular, tablet, onda sonora, sol), todos com o mesmo peso de traço e tamanho, alinhados em linha horizontal com espaçamento regular sobre fundo cinza claro, sem texto de legenda visível. Nos quadros finais (q0008, q0009), texto sobreposto direto na parede de madeira atrás do apresentador funciona como marcador de agenda: hierarquia por peso e cor, item atual em preto e negrito, itens anteriores em cinza claro menor, empilhados verticalmente à esquerda.
- Mudança entre quadros: de q0001 para q0002, o logo estático da Apple é substituído por uma forma de anel colorido em transição de abertura. De q0002 para q0003, a tela de abertura dá lugar ao apresentador em vídeo real. De q0004 para q0005, a cena corta do apresentador para um painel vazio que começa a preencher com os quatro ícones azuis à direita; q0006 repete a mesma composição de ícones. De q0007 para q0008, aparece o texto "Symbol effects" em cinza com "UI framework APIs" em preto sobrepostos à parede. De q0008 para q0009, um terceiro item, "Adoption tips", é adicionado abaixo dos dois anteriores, com "UI framework APIs" agora em cinza e "Adoption tips" em preto, indicando avanço de uma lista de agenda item a item.
- Com a fala: enquanto o apresentador se apresenta e diz que os símbolos SF vão ganhar animação em iOS 17 e macOS Sonoma, a tela mostra a abertura da sessão, um painel de ícones de contextos de uso (toolbars, notificações, mídia, brilho) e depois a lista de agenda da talk sendo construída item por item, em sincronia com o roteiro que ele descreve verbalmente (tour de animações, APIs, dicas de adoção).

### wwdc2023_10258 · folha 0002 · de 01:12 a 02:34 · código 8FBA2
- Tela: painel claro com grade de ícones azuis de símbolos, dispostos em duas linhas: linha de cima com quatro ícones (celular com ondas, tablet com pessoa, onda sonora, duas fotos sobrepostas) rotulados abaixo como "Bounce", "Pulse", "Variable Color", "Scale"; linha de baixo com três ícones (envelope com bolinha, outro envelope com bolinha, sol) rotulados "Appear", "Disappear", "Replace". Em seguida, dois quadros de título simples só com texto ("Symbol effects", depois "Symbols framework") sobre fundo cinza claro, e o apresentador voltando a aparecer na mesma sala. Nos últimos quadros, um ícone azul de "foto genérica" (moldura com sol e montanha) aparece com o rótulo de código "BounceSymbolEffect" embaixo, depois o mesmo ícone se duplica lado a lado com rótulos ".bounce.up" e ".bounce.down", e por fim volta a um único ícone central rotulado ".bounce".
- Construção de interface: grade de 4 colunas por 2 linhas, ícones do mesmo peso de traço e tamanho, rótulo em texto pequeno centralizado abaixo de cada ícone, tudo sobre fundo neutro sem bordas ou cartões, layout típico de slide de apresentação. No quadro q0012, aparece um rodapé fino de rodapé com "What's new in SF Symbols 5" à esquerda e "WWDC23" à direita, separado por uma linha horizontal fina, indicando crédito ou referência cruzada de sessão. Nos quadros de código, o texto do nome do efeito aparece em fonte monoespaçada abaixo do ícone, com hierarquia de tamanho e posição (nome do efeito sempre abaixo da imagem representativa).
- Mudança entre quadros: q0010 e q0011 mostram a mesma grade de sete ícones, praticamente sem diferença perceptível entre os dois. De q0011 para q0012, a grade é reduzida e reorganizada (quatro ícones em cima, três embaixo já sem o quarto da segunda linha) e ganha um rodapé de crédito que não existia antes; o ícone de "Appear" muda de desenho, de um envelope com notificação para um símbolo de ondas de sinal. De q0012 para q0013, a tela passa da grade de ícones para um slide de texto simples. De q0013 para q0014, o título do slide muda de "Symbol effects" para "Symbols framework". De q0016 para q0017, o ícone único de foto rotulado "BounceSymbolEffect" se transforma em dois ícones idênticos lado a lado, cada um com um rótulo de variação (".bounce.up" e ".bounce.down"). De q0017 para q0018, os dois ícones voltam a se fundir em um único ícone central, agora rotulado apenas ".bounce".
- Com a fala: enquanto a fala descreve os sete tipos de animação (Bounce, Pulse, Variable Color, Scale, Appear, Disappear, Replace) e explica que cada efeito tem um nome separado por ponto no código, como ".bounce", a tela mostra exatamente essa grade de ícones rotulados e, na sequência, a notação de código com variações de direção (".bounce.up", ".bounce.down") convergindo para o nome base.

### wwdc2023_10258 · folha 0003 · de 02:37 a 03:52 · código AB160
- Tela: apresentador segue falando na mesma sala de madeira. Um quadro mostra um slide com uma linha de código centralizada em fonte monoespaçada, ".variableColor.iterative.hideInactiveLayers.nonReversing", com uma bolinha azul acima do texto (indicador de reprodução) sobre fundo cinza claro. Dois quadros mostram uma captura de tela do aplicativo SF Symbols no macOS, com barra de título da janela, um seletor de peso de fonte ("SF Pro", "Regular"), uma grade de símbolos relacionados a ondas de rádio, carros e estacionamento, e um painel lateral direito de configuração chamado "Variable Color" com opções "Style" (Cumulative/Iterative), "Inactive Layers" (Dim/Hide) e "Reversing", além de uma prévia de cinco variações do símbolo selecionado no topo do painel. Os últimos quadros voltam a ícones azuis únicos e centralizados, primeiro rotulado "Bounce" (celular com ondas), depois "Scale" (duas fotos sobrepostas).
- Construção de interface: a janela do app SF Symbols segue o padrão de app de macOS, com barra de ferramentas superior, ícones de visualização (grade, lista) e busca à direita; o painel de configuração à direita usa radio buttons agrupados por categoria, com rótulos alinhados à esquerda e opções à direita, e o símbolo em edição destacado por uma borda azul de seleção na grade principal. O slide de código usa fonte monoespaçada centralizada com espaçamento generoso, sem elementos decorativos.
- Mudança entre quadros: de q0019 para q0020, o apresentador dá lugar ao slide com a linha de código completa e o indicador de reprodução azul; de q0020 para q0021, a tela retorna ao apresentador na mesma pose. De q0023 para q0024, dentro do app SF Symbols, o símbolo selecionado no painel lateral permanece o mesmo, mas os cinco ícones de prévia de animação no topo do painel mudam o padrão de preenchimento entre os dois quadros, mostrando estágios diferentes da animação de Variable Color. De q0025 para q0026, a tela corta do apresentador para um slide de ícone único "Bounce"; de q0026 para q0027, o ícone de "Bounce" é substituído pelo ícone de "Scale".
- Com a fala: a fala explica a sintaxe encadeada do efeito Variable Color (opções iterative, hideInactiveLayers, reversing) e recomenda o app SF Symbols para explorar as animações disponíveis; a tela mostra literalmente essa string de código e, em seguida, a interface real do app SF Symbols com o painel de configuração de Variable Color, confirmando visualmente os mesmos parâmetros citados na fala.
### wwdc2023_10258 · folha 0004 · de 04:04 a 05:34 · código 44F33
- Tela: slides de fundo cinza claro com ícones azuis rotulados ("Scale" com etiqueta cinza "Indefinite" abaixo; "Appear & Disappear" com etiqueta "Transition" abaixo). Um slide de texto puro lista verticalmente à direita quatro categorias: "Discrete", "Indefinite", "Transition", "Content Transition". Outro slide lista os nomes de protocolo em fonte monoespaçada: "DiscreteSymbolEffect", "IndefiniteSymbolEffect", "TransitionSymbolEffect", "ContentTransitionSymbolEffect". Depois aparece uma tabela com colunas "Discrete", "Indefinite", "Transition", "Content Transition" e linhas para cada efeito (Bounce, Pulse, Variable Color, Scale, Appear, Disappear, Replace), com marcas de check verdes indicando quais comportamentos cada efeito suporta. O apresentador reaparece com os textos de agenda "Symbol effects" (cinza) e "UI framework APIs" (preto, negrito) e "Adoption tips" (cinza) sobrepostos à parede atrás dele. Os últimos quadros mostram blocos de código lado a lado: comentário "// Symbol effects in SwiftUI" com a linha `Image(systemName: "wifi.router").symbolEffect()`, e comentário "// Symbol effects in AppKit and UIKit" com `let imageView: NSImageView = ...` e `imageView.addSymbolEffect(.variableColor)`, ao lado de um ícone azul de roteador wifi com ondas de sinal.
- Construção de interface: a tabela usa linhas zebradas sutis, cabeçalho em texto menor e cinza, células de dado alinhadas ao centro, ícone de check verde circular como único elemento colorido na tabela monocromática. Os blocos de código usam realce de sintaxe: palavras-chave e tipos em roxo ou azul (`Image`, `NSImageView`), nomes de parâmetro e string em laranja ou verde, comentários em cinza itálico-claro, fonte monoespaçada em todos os blocos.
- Mudança entre quadros: de q0028 para q0029, o slide muda do ícone "Scale" (Indefinite) para o ícone "Appear & Disappear" (Transition). De q0029 para q0030, a tela passa de ícones para uma lista de texto simples com as quatro categorias de comportamento. De q0031 para q0032, a lista de nomes de protocolo dá lugar à tabela de compatibilidade completa com os checks verdes. De q0032 para q0033, a tabela é substituída pelo apresentador com o texto de agenda mostrando "UI framework APIs" agora em destaque preto. De q0034 para q0035, no bloco de código SwiftUI, o parâmetro vazio de `.symbolEffect()` passa a conter `.variableColor` destacado em azul claro. De q0035 para q0036, é adicionado um segundo bloco de código abaixo, para AppKit e UIKit, com `imageView.addSymbolEffect(.variableColor)`.
- Com a fala: a fala explica os quatro tipos de comportamento (discreto, indefinido, transição, transição de conteúdo) e como cada efeito se declara conforme protocolos; a tela mostra exatamente essa classificação em lista, depois em nomes de protocolo Swift e por fim na tabela de compatibilidade, seguida do primeiro exemplo de código real do modificador `symbolEffect` em SwiftUI e do método equivalente em AppKit/UIKit.

### wwdc2023_10258 · folha 0005 · de 05:46 a 07:04 · código 2996F
- Tela: blocos de código lado a lado (SwiftUI acima, AppKit/UIKit abaixo) evoluindo em complexidade, sempre ao lado de um ícone azul de roteador wifi com ondas de sinal à direita. Um slide de título "Indefinite effects" mostra uma grade de quatro ícones azuis rotulados: "PulseSymbolEffect", "VariableColorSymbolEffect", "ScaleSymbolEffect" e "AppearSymbolEffect / DisappearSymbolEffect" (dois nomes sob um único ícone). O apresentador aparece entre os blocos de código, na mesma sala de mesa de madeira.
- Construção de interface: os blocos de código mantêm o mesmo destaque de sintaxe (roxo para tipos, azul claro para o parâmetro do efeito, verde ou laranja para strings), com o parâmetro sendo destacado por um retângulo de fundo azul claro quando é o foco da explicação (ex.: ".variableColor.iterative.reversing" e depois "isActive: isConnectingToInternet"). O ícone de roteador wifi muda de cor entre azul sólido e azul claro/apagado conforme o estado do código (ativo vs. inativo), funcionando como demonstração visual do efeito indefinido ligando e desligando.
- Mudança entre quadros: de q0037 para q0038, o texto do parâmetro do efeito muda de ".variableColor.iterative.reversing" (igual) mas o ícone de roteador aparece mais apagado/claro em q0038 comparado a q0037, sugerindo variação do estado da animação. De q0038 para q0039, é adicionada uma segunda chamada de efeito encadeada, `.symbolEffect(.scale.up)`, tanto no bloco SwiftUI quanto no AppKit/UIKit, e o ícone de roteador aparece maior e mais escuro. De q0040 para q0041, a tela corta do apresentador para o slide de grade "Indefinite effects" com os quatro ícones nomeados. De q0041 para q0042, o slide dá lugar novamente ao apresentador e depois a um bloco de código único de "Indefinite symbol effects in SwiftUI". De q0043 para q0044, o código ganha um parâmetro adicional `isActive: isConnectingToInternet` dentro de um wrapper `.symbolEffect(...)`, e o ícone de roteador muda de apagado (q0043) para azul sólido brilhante (q0044), mostrando o efeito ativo. De q0044 para q0045, a mesma estrutura de código se repete com o ícone voltando a um tom mais claro.
- Com a fala: a fala explica como combinar múltiplos efeitos indefinidos (variableColor mais scale.up) e como controlar sua ativação com o parâmetro booleano `isActive`, usando o exemplo de conexão à internet; a tela mostra literalmente essa evolução do código e o ícone de wifi alternando de estado ativo para inativo em sincronia com a explicação.

### wwdc2023_10258 · folha 0006 · de 07:14 a 08:39 · código 58233
- Tela: o apresentador fala, intercalado com um slide de título "Discrete effects" mostrando um ícone azul de "foto genérica" rotulado "BounceSymbolEffect". Em seguida, uma sequência de blocos de código em SwiftUI dentro de uma `VStack`, com uma `Image` de antena de rádio (`antenna.radiowaves.left.and.right`) usando `.symbolEffect(.bounce, value: bounceValue)`, um `Button("Animate")` que incrementa `bounceValue`, e um botão retangular azul-claro rotulado "Animate" desenhado ao lado do ícone de antena. O código evolui para incluir também a versão AppKit/UIKit (`imageView.addSymbolEffect(.bounce)`) e depois uma variação com `options: .repeat(2)` para repetir o efeito duas vezes.
- Construção de interface: o botão "Animate" é desenhado como um retângulo de cantos levemente arredondados, fundo azul muito claro, texto azul mais escuro centralizado, imitando um controle de botão real de UI ao lado do bloco de código que o gera. O ícone de antena de rádio usa o mesmo estilo de traço azul consistente com os demais símbolos do vídeo. Hierarquia do código: comentário cinza no topo identificando a plataforma, bloco de estrutura (VStack, Button) com indentação clara, parâmetros de efeito destacados.
- Mudança entre quadros: de q0046 para q0047, o apresentador dá lugar ao slide "Discrete effects" com o ícone "BounceSymbolEffect". De q0047 para q0048, o slide é substituído pelo apresentador e depois por um bloco de código com a antena de rádio e o parâmetro `value: bounceValue`. De q0048 para q0049, o layout se expande para incluir a `VStack` completa com o botão "Animate" desenhado visualmente ao lado do código. De q0049 para q0050, o trecho `bounceValue += 1` dentro do botão é destacado com fundo cinza-claro, chamando atenção para essa linha específica. De q0050 para q0051, é adicionado um segundo bloco de código abaixo para AppKit/UIKit com `imageView.addSymbolEffect(.bounce)` comentado como "Bounce once". De q0052 para q0053, o parâmetro `options: .repeat(2)` é inserido dentro do `.symbolEffect(.bounce, ...)`, tanto na versão SwiftUI quanto na AppKit/UIKit (comentário muda para "Bounce twice"). De q0053 para q0054, a tela corta de volta ao slide "Discrete effects" com o apresentador entrando em quadro pela direita.
- Com a fala: a fala explica como disparar efeitos discretos como Bounce a partir de um botão que incrementa um valor de estado, e como configurar repetição com `options: .repeat(2)` para o símbolo saltar duas vezes; a tela mostra exatamente esse código sendo construído passo a passo em SwiftUI e depois replicado em AppKit/UIKit.
### wwdc2023_10258 · folha 0007 · de 08:51 a 10:17 · código 3BCF9
- Tela: slide "Discrete effects" com três ícones azuis em grade: "BounceSymbolEffect" (foto genérica), "PulseSymbolEffect" (balão de fala), "VariableColorSymbolEffect" (onda sonora vertical). Segue bloco de código completo em SwiftUI, com `VStack`, `Image` de antena de rádio usando `.symbolEffect(.variableColor.iterative, options: .repeat(2), value: discreteValue)`, botão "Animate" desenhado à direita do ícone de antena, e abaixo o equivalente AppKit/UIKit com comentário "Animate twice". Depois, blocos de "Content transition symbol effects in SwiftUI": `Image(systemName: isPaused ? "pause.fill" : "play.fill")` com `.contentTransition(.symbolEffect(.replace.offUp))`, ao lado de um ícone azul grande de "play" triangular. Um bloco final mostra um `Button` completo que alterna `isPaused.toggle()` com o ícone de play/pause dentro. Por fim, slide de título "Transition effects" com dois ícones: "AppearSymbolEffect" e "DisappearSymbolEffect" (nuvem de chuva com sol).
- Construção de interface: o ícone de "play" azul sólido em forma de triângulo aparece isolado, grande, centralizado, representando o resultado visual do código de transição de conteúdo ao lado; o mesmo padrão de código com comentário de plataforma acima de cada bloco se mantém.
- Mudança entre quadros: de q0055 para q0056, o slide de três ícones "Discrete effects" dá lugar ao apresentador. De q0057, o código já contém a chamada combinada `.variableColor.iterative` com `options: .repeat(2)` e o botão "Animate" desenhado ao lado do ícone de antena. De q0058 para q0059, a tela passa do apresentador para o primeiro bloco de código de "Content transition", sem o ícone de play ainda visível de forma protagonista, aparecendo o ícone de play centralizado maior. De q0059 para q0060, é adicionada a linha `.contentTransition(.symbolEffect(.replace.offUp))` ligada à imagem condicional. De q0060 para q0061, o bloco de código é reescrito dentro de um `Button` com `isPaused.toggle()` e o rótulo com a imagem condicional. De q0062 para q0063, a tela corta do apresentador para o slide "Transition effects" com os dois ícones de Appear/Disappear.
- Com a fala: a fala mostra como combinar `variableColor` com repetição customizada e introduz o efeito Replace como transição de conteúdo entre dois símbolos (pause/play) dentro de um botão que alterna estado; a tela demonstra esse código exatamente na ordem descrita, terminando com a introdução do próximo tema, os efeitos de transição Appear e Disappear.

### wwdc2023_10258 · folha 0008 · de 10:29 a 12:03 · código 60467
- Tela: slides de título "Appear and Disappear" repetidos, com uma composição de três formas cinza (quadrado arredondado, símbolo de lua com estrelas, círculo) representando itens de uma barra ou fileira de elementos. Progressivamente, o ícone da lua aparece em azul entre os dois elementos cinza. Blocos de código em SwiftUI dentro de um `HStack` com `RoundedRectangle(...)`, `Image(systemName: "moon.stars").symbolEffect(.disappear, isActive: isMoonHidden)` e `Circle(...)`, com o código equivalente em AppKit/UIKit usando `imageView.addSymbolEffect(.disappear)` e depois `.appear` para reaparecer. No final da folha, o ícone da lua aparece esmaecido/translúcido ao lado de uma etiqueta cinza "Indefinite".
- Construção de interface: a composição de três formas (quadrado, símbolo central, círculo) serve como diagrama abstrato de uma fileira de ícones de interface, usada para demonstrar se o espaçamento entre os elementos vizinhos muda ou não quando o símbolo central desaparece; o quadrado e o círculo permanecem cinza sólido como referência fixa de layout, enquanto o símbolo central muda de opacidade e cor.
- Mudança entre quadros: de q0064 para q0065, a tela passa do apresentador para o slide "Appear and Disappear" com apenas o quadrado e o círculo cinza, sem o ícone central ainda. De q0066 para q0067, o ícone azul de lua com estrelas aparece centralizado entre as duas formas cinza. De q0067 para q0068, é adicionado o bloco de código SwiftUI completo ao lado esquerdo da composição. De q0068 para q0069, o código ganha a seção equivalente de AppKit/UIKit abaixo, com `addSymbolEffect(.disappear)` e o comentário "Re-appear the symbol" seguido de `.appear`. De q0070 para q0071, a composição de ícones se repete três vezes na mesma tela (três estados lado a lado), com o ícone da lua ficando progressivamente mais claro/translúcido da esquerda para a direita, e uma etiqueta "Indefinite" aparece abaixo do terceiro estado. De q0071 para q0072, a tela corta de volta para o apresentador.
- Com a fala: a fala explica o comportamento indefinido de Appear/Disappear, no qual o símbolo desaparece visualmente mas continua ocupando espaço na hierarquia (sem alterar o layout ao redor); a tela demonstra isso com o ícone da lua sumindo gradualmente enquanto o quadrado e o círculo vizinhos permanecem na mesma posição, sem se mover.

### wwdc2023_10258 · folha 0009 · de 12:13 a 13:32 · código D2162
- Tela: continuação dos blocos de código "Appear and disappear in SwiftUI" dentro de um `HStack`, evoluindo para incluir uma condicional `if !isMoonHidden { Image(...) }` que insere e remove a imagem da hierarquia, com `.transition(.symbolEffect(.disappear.down))` e depois `.transition(.symbolEffect(.automatic))`. Ao lado, a composição de quadrado, lua e círculo se repete, com o círculo mudando de posição horizontal (mais próximo do quadrado) quando a lua não está presente, mostrando reflow de layout. Aparece também o bloco AppKit/UIKit com um closure de conclusão: `imageView.addSymbolEffect(.disappear) { context in if let imageView = context.sender as? UIImageView, context.isFinished { imageView.removeFromSuperview() } }`. O apresentador reaparece com o texto de agenda mostrando "Adoption tips" agora em destaque preto. Por fim, um slide "Bar button items" mostra uma barra de ferramentas horizontal com ícones azuis (pasta com "+", carro, "Select" em texto) e um campo de busca com ícone de lupa e microfone à direita.
- Construção de interface: a barra de ferramentas do slide final segue o padrão visual de uma bar UIKit real, com ícones à esquerda alinhados horizontalmente, um rótulo de texto ("Select") no meio e um campo de busca arredondado à direita contendo ícone de lupa e ícone de microfone, todo em tons de azul e cinza claros sobre fundo branco.
- Mudança entre quadros: de q0073 para q0074, o código muda de usar `isActive: isMoonHidden` para uma estrutura condicional `if !isMoonHidden { Image(...) }`, e a posição do círculo na composição ao lado se aproxima do quadrado, mostrando que agora o layout reflui quando o símbolo é removido. De q0074 para q0075, é adicionado `.transition(.symbolEffect(.disappear.down))` à imagem condicional. De q0076 para q0077, o parâmetro do efeito muda de `.disappear.down` para `.automatic`, e é adicionado o bloco AppKit/UIKit com o closure de conclusão contendo `removeFromSuperview()`. De q0079 para q0080, a tela corta do apresentador de volta a ele mesmo com o texto de agenda "Adoption tips" agora em preto negrito (era cinza antes). De q0080 para q0081, a tela passa do apresentador para o slide "Bar button items" com a barra de ferramentas ilustrativa.
- Com a fala: a fala explica a diferença entre o comportamento indefinido (sem mudança de layout) e o comportamento de transição (com mudança de layout, via inserção/remoção condicional da view) e o uso do transition automático e do completion handler em UIKit para remover a view após a animação; a tela mostra a evolução exata desse código e a mudança de posição do círculo, e depois introduz o próximo tópico de dicas de adoção com o exemplo de barra de ferramentas.
### wwdc2023_10258 · folha 0010 · de 13:44 a 14:49 · código 85D3E
- Tela: slide "Bar button items" com a mesma barra de ferramentas do fim da folha anterior. Slide "UIControl symbol animations" com um controle deslizante (slider) de brilho, ícone de sol pequeno à esquerda, sol grande à direita, alça branca circular sobre a trilha azul; o mesmo slide reaparece com uma linha adicional de texto "Opt in/out with `isSymbolAnimationEnabled`" e a alça do slider em nova posição, mais próxima do centro. Slide "Symbol effect propagation" primeiro sem conteúdo abaixo do título e depois com um bloco de código mostrando uma `VStack` contendo três `Image(systemName: "...")` e um único modificador `.symbolEffect(.pulse)` aplicado por fora do stack inteiro. Intercalado, o apresentador aparece falando na mesma sala.
- Construção de interface: o slider de brilho segue o desenho padrão de controle do sistema, com trilha em duas cores (azul preenchido à esquerda da alça, cinza claro à direita), alça circular branca com sombra sutil, ícones de contexto (sol pequeno/grande) posicionados nas extremidades para indicar a escala do valor.
- Mudança entre quadros: de q0082 para q0083, o slide "Bar button items" dá lugar ao apresentador. De q0083 para q0084, aparece o slide "UIControl symbol animations" com o subtítulo em texto claro e o slider já com a alça deslocada para a direita. De q0084 para q0085, é adicionada a segunda linha de texto sobre `isSymbolAnimationEnabled`, e a alça do slider muda de posição (mais recuada). De q0085 para q0086, a tela corta para o apresentador. De q0087 para q0088, o slide "Symbol effect propagation" ganha o bloco de código com a `VStack` de três imagens e o modificador `.pulse` aplicado uma única vez no nível do stack.
- Com a fala: a fala descreve que controles do UIKit como UISlider já têm animação de símbolo embutida (bounce ao atingir os limites), controlável via `isSymbolAnimationEnabled`, e que o modificador `symbolEffect` se propaga pela hierarquia de views quando aplicado a uma view pai; a tela demonstra o slider de brilho como exemplo desse controle e o código mostrando o efeito aplicado uma única vez a um `VStack` com três imagens.

### wwdc2023_10258 · folha 0011 · de 14:54 a 16:19 · código 7FF5D
- Tela: blocos de código "Effects without animation in SwiftUI" com `Image(systemName: "iphone.radiowaves.left.and.right")`, `.symbolEffect(.scale.up, isActive: isScaledUp)` e um `.onAppear` contendo `var transaction = Transaction()`, `transaction.disablesAnimations = true`, `withTransaction(transaction) { isScaledUp = true }`. Depois, slide "Variable value" com um ícone de alto-falante apagado/translúcido, e no quadro seguinte quatro ícones idênticos de alto-falante com ondas sonoras crescentes, todos coloridos e bem visíveis, ilustrando níveis progressivos de volume. Segue bloco de código "Variable value animations in SwiftUI" com `Image(systemName: "wifi", variableValue: signalLevel)` ao lado de um ícone de wifi azul com três barras cheias. Nos últimos quadros, o código ganha a seção AppKit/UIKit com `let currentSignalImage = UIImage(systemName: "wifi", variableValue: signalLevel)` e `imageView.setSymbolImage(currentSignalImage, contentTransition: .automatic)`, com o ícone de wifi ao lado alternando entre estado cheio (azul sólido) e um estado mais claro/translúcido.
- Construção de interface: os quatro ícones de alto-falante na tela "Variable value" formam uma sequência visual crescente de intensidade (uma onda, depois duas, três), todos com o mesmo estilo de traço e cor azul, dispostos em linha horizontal com espaçamento igual, funcionando como demonstração do conceito de "variable value" antes mesmo de mostrar código.
- Mudança entre quadros: de q0091 para q0092, a linha `isScaledUp = true` dentro do `withTransaction` é destacada com fundo cinza-claro. De q0092 para q0093, a tela corta para o apresentador. De q0094 para q0095, o ícone único de alto-falante apagado é substituído por quatro ícones de alto-falante com volumes crescentes, todos visíveis e coloridos. De q0096 para q0097, o código evolui de uma linha só (`Image(...variableValue: signalLevel)`) para incluir o ícone de wifi ao lado, com o texto do parâmetro `signalLevel` destacado. De q0097 para q0098, é adicionado o bloco de código AppKit/UIKit completo abaixo, e o ícone de wifi ao lado muda de posição vertical (mais alto na tela). De q0098 para q0099, o ícone de wifi muda de azul sólido para um tom mais claro/translúcido, mostrando a transição entre dois níveis de sinal.
- Com a fala: a fala explica como aplicar efeitos sem animação usando uma `Transaction` com `disablesAnimations`, e depois introduz o conceito de variable value (usado em ícones de volume e sinal), mostrando que em SwiftUI a atualização é automática e em AppKit/UIKit se usa `setSymbolImage` com `contentTransition: .automatic` para crossfade entre valores; a tela ilustra exatamente essa progressão, do código de transação sem animação até o ícone de wifi mudando de nível de sinal.

### wwdc2023_10258 · folha 0012 · de 16:21 a 17:02 · código 74EF5
- Tela: cena de encerramento com uma mesa de madeira clara, uma tigela de vidro com balas de goma coloridas e um cubo mágico colorido (azul, vermelho, amarelo) no canto superior direito, sem o apresentador em quadro. Sobre essa mesma cena de fundo, aparece um cartão "Next steps" com três itens de texto listados progressivamente: "Discover animations in the SF Symbols app", "Explore the symbol effects API", "Adopt symbol animations in your apps". No último quadro desse cartão, uma lista de duas sessões relacionadas aparece abaixo de uma linha divisória fina: "What's new in SF Symbols 5" e "Create animated symbols", cada uma com o rótulo "WWDC23" alinhado à direita. Os dois quadros finais mostram apenas o logotipo da Apple em branco ao lado do texto "WWDC23" sobre fundo preto.
- Construção de interface: o cartão "Next steps" usa tipografia preta sobre fundo claro de madeira desfocada, título em negrito maior e itens de lista em peso regular menor; a lista de sessões relacionadas usa uma linha divisória horizontal fina acima de cada referência, com o nome da sessão à esquerda e o crédito "WWDC23" em cinza à direita, mesmo padrão observado na folha 0002.
- Mudança entre quadros: de q0100 para q0101, a cena de fundo (mesa com balas e cubo mágico) permanece igual, mas surge o cartão "Next steps" com apenas dois itens listados. De q0101 para q0102, um terceiro item, "Adopt symbol animations in your apps", é adicionado à lista. De q0102 para q0103, a lista de próximos passos ganha, abaixo de uma linha divisória, duas referências de sessões relacionadas com seus rótulos "WWDC23". De q0103 para q0104, a tela de encerramento com fundo de madeira é substituída por uma tela preta com o logotipo da Apple e "WWDC23" à esquerda; de q0104 para q0105, um segundo logotipo idêntico aparece à direita, duplicando a marca na tela final.
- Com a fala: a fala agradece a audiência e recapitula os próximos passos sugeridos (explorar o app SF Symbols, a API de symbol effects e adotar as animações nos apps), mencionando outras sessões sobre diretrizes de interface e símbolos customizados; a tela mostra exatamente essa lista de próximos passos e as duas sessões relacionadas, encerrando com a marca da conferência.

## Síntese visual (folhas 0001 a 0012)
- Identidade visual consistente de ícones: todos os símbolos de exemplo (celular, tablet, onda sonora, sol, envelope, foto genérica, antena, balão de fala, lua, nuvem, alto-falante, wifi, roteador) usam o mesmo azul de sistema, mesmo peso de traço e mesmo tamanho relativo em toda a apresentação, reforçando que são o mesmo componente (SF Symbol) em contextos diferentes (folhas 0001, 0002, 0004, 0005, 0007, 0011). A fala nomeia os efeitos, mas é a imagem que revela a linguagem visual unificada dos ícones.
- Agenda construída como elemento cênico, não como slide isolado: o texto de agenda ("Symbol effects", "UI framework APIs", "Adoption tips") é sobreposto diretamente na parede de madeira ripada atrás do apresentador, com o item atual em preto negrito e os demais em cinza claro, reaparecendo em três momentos da talk para marcar transição de bloco (folhas 0001, 0003, 0009). Isso não é mencionado na fala, que apenas anuncia verbalmente os tópicos.
- Tabela de compatibilidade como resumo visual denso: a matriz de efeitos por comportamento (Discrete, Indefinite, Transition, Content Transition) com checks verdes (folha 0004) condensa em uma imagem só o que a fala explica ao longo de vários minutos, funcionando como referência rápida que não tem equivalente sonoro direto.
- Padrão de exibição de código pareado por plataforma: sempre que uma funcionalidade é mostrada, o código SwiftUI aparece primeiro (bloco superior) e o equivalente AppKit/UIKit aparece logo abaixo (bloco inferior), com comentários de cabeçalho identificando a plataforma; esse pareamento se repete em praticamente todas as folhas de código (0004 a 0011), mostrando a paridade de API como decisão deliberada de design da documentação.
- Diagramas abstratos para explicar layout: a composição de três formas simples (quadrado, símbolo central, círculo) é usada como diagrama de fileira de interface para mostrar visualmente se elementos vizinhos se movem ou não quando um símbolo aparece/desaparece (folhas 0008 e 0009); a posição do círculo muda fisicamente entre quadros para tornar concreta a diferença entre efeito indefinido e efeito de transição, algo que a fala descreve de forma abstrata ("parallel universes").
- Progressão incremental de código em blocos de tela fixos: em vários trechos (folhas 0006, 0007, 0009, 0010, 0011) o mesmo bloco de código é reexibido quase idêntico entre quadros consecutivos, com apenas uma linha nova ou um trecho destacado em cinza-claro a cada mudança, revelando um padrão didático de "build" de código onde cada conceito é adicionado isoladamente antes de compor o exemplo completo.
- Interface real do macOS como prova de conceito: as únicas capturas de tela de um app real (SF Symbols, folha 0003) mostram um painel de configuração com radio buttons e uma prévia de cinco variações do símbolo, evidenciando como a ferramenta de design de símbolos expõe visualmente as mesmas opções descritas em código, e não apenas ícones estilizados de slide.
- Encerramento ancorado em objetos físicos da mesa: a cena final (folha 0012) reaproveita os mesmos objetos de cena vistos ao longo do vídeo (cubo mágico colorido, agora acompanhado de uma tigela de balas de goma), sem o apresentador, como pano de fundo para a lista de próximos passos, criando continuidade visual com a cenografia usada durante toda a apresentação.
