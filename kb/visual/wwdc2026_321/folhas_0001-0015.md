### wwdc2026_321 · folha 0001 · de 00:00 a 01:10 · código 26C05
- Tela: abertura com card laranja "WWDC26" (q0001), apresentador falando em estúdio com parede vermelho escuro e mesas altas ao fundo (q0002 a q0004), depois slide de título "Lazy stacks" (q0005) que evolui mostrando dois itens de agenda "Reordering" com selo verde "NEW" e "Swipe actions" esmaecido/em transição, com rodapé "Code-along: Build powerful drag and drop in SwiftUI · WWDC26" (q0006). Segue apresentador de olhos fechados falando (q0007, q0008), e slide "Prerequisites" vazio (q0009).
- Construção de interface: slide de agenda com título grande em negrito no topo esquerdo, itens de lista com selo (badge) verde arredondado contendo "NEW" em texto branco ao lado do item "Reordering". Rodapé do slide com referência a outra sessão e a marca "WWDC26" alinhada à direita, tipografia pequena cinza.
- Mudança entre quadros: de q0005 para q0006, o item "Reordering" com selo verde "NEW" aparece nitidamente, enquanto um segundo item abaixo ("Swipe actions") aparece esmaecido, como se estivesse surgindo em transição de fade-in.
- Com a fala: enquanto o apresentador se apresenta como engenheiro de UI Frameworks e menciona que dá para usar "reorderable" para arrastar e reordenar views, e que SwiftUI permite swipe actions fora de List, a tela mostra a agenda do vídeo listando exatamente esses dois tópicos, "Reordering" (destacado como novo) e "Swipe actions".

### wwdc2026_321 · folha 0002 · de 01:22 a 02:34 · código 4B925
- Tela: slide "Prerequisites" com item "Layout using stacks" e referência de rodapé "Stacks, Grids, and Outlines in SwiftUI · WWDC20" (q0010). Depois bloco de código Swift lado a lado com moldura de iPhone mostrando o app "Swan" com três cartões de passo de origami (imagem de dobradura + título "Fold and unfold" / "Fold twice" / "Fold" + texto explicativo) dentro de scroll view (q0011, q0012). Seguem três diagramas simplificados do telefone com os mesmos três cartões de conteúdo (q0013, q0014, quase idênticos), e depois versões abstratas simplificadas (barras retangulares cinza representando o conteúdo) rotuladas "LazyVStack", com contorno laranja ao redor da parte "Visible" do topo (q0015 a q0018), a última com marcação "Estimated" indicando uma área inferior maior sem contorno.
- Construção de interface: código Swift com destaque de sintaxe (palavras-chave em rosa/roxo, tipos em verde, comentário em cinza), estrutura `ContentView` contendo `ScrollView` > `LazyVStack` > `ForEach(steps)` > `StepView`. Moldura de iPhone realista com barra de status (hora "9:41", ícones de sinal, wifi, bateria). Tela do app mostra três cartões empilhados verticalmente, cada um com ilustração de origami em fundo azul claro, título em negrito preto e parágrafo de instrução em cinza menor. Diagrama abstrato reduz os cartões a barras cinza retangulares de larguras variadas simulando texto, dentro de um retângulo alongado vertical representando a tela; um contorno laranja marca a área "Visible" no topo, e uma marcação de régua vertical "Estimated" indica altura estimada da área abaixo, ainda não carregada.
- Mudança entre quadros: de q0013 para q0014, os cartões dentro do telefone mudam de posição de leve rolagem (mesmo conteúdo, mesma disposição, indicando froze de estado ou rolagem mínima). De q0014 para q0015, a tela passa da moldura de iPhone completa para o diagrama abstrato simplificado ao lado, rotulado "LazyVStack", com contorno laranja envolvendo os primeiros itens visíveis. De q0016 para q0017 para q0018, o diagrama abstrato se repete quase idêntico, mantendo o contorno laranja na mesma posição de topo. De q0018, aparece uma marcação de régua vertical rotulada "Estimated" ao lado da área inferior sem contorno, abaixo dos itens visíveis.
- Com a fala: a fala descreve o código de configuração do app de origami com `ScrollView` contendo `LazyVStack` e `ForEach` de `StepView`, e explica que a LazyVStack carrega vistas conforme necessário e estima a altura das partes não carregadas; a tela mostra exatamente esse código, o app renderizado num iPhone, e depois o diagrama abstrato com a área "Visible" marcada em laranja e a área "Estimated" indicada por uma régua, ilustrando a estimativa de altura das views fora de tela.

### wwdc2026_321 · folha 0003 · de 02:49 a 03:58 · código 9B867
- Tela: diagramas abstratos repetidos do "LazyVStack" (barras cinza dentro de retângulo vertical, contorno laranja na área visível), com rótulo "Width" e uma seta horizontal dupla sob o retângulo indicando a largura do stack (q0019). Apresentador falando (q0020, q0022). Mais diagramas abstratos similares (q0021, q0023, q0024). Diagrama com seta vertical rotulada "Content offset" apontando para baixo ao lado do retângulo, com contorno laranja mais abaixo na pilha (q0025). Depois um iPhone em orientação retrato mostrando o app com dois cartões de instrução de origami maiores, ao lado de diagrama abstrato equivalente (q0026, q0027).
- Construção de interface: seta de medida horizontal de duas pontas sob o diagrama, rotulada "Width", indicando anotação de medida de layout. Seta vertical rotulada "Content offset" à esquerda do retângulo indicando a posição de rolagem acima da área visível. Nos quadros com iPhone, tela mostra dois cartões grandes de instrução (título em negrito, imagem de dobradura em fundo azul claro, texto explicativo abaixo), maior que nos quadros da folha 0002, sugerindo cartões em outra proporção ou orientação.
- Mudança entre quadros: de q0019 para q0021, o rótulo "Width" com seta desaparece e o diagrama volta a mostrar só o retângulo com contorno laranja, sem anotação de medida. De q0023 para q0024, o retângulo cinza perde as barras internas (fica em branco/vazio), enquanto o retângulo ao lado mantém o contorno laranja com conteúdo, sugerindo comparação entre dois estados de carregamento. De q0025, aparece uma seta vertical "Content offset" nova, não presente nos quadros anteriores desta folha. De q0025 para q0026, a cena muda de diagrama abstrato para captura de iPhone real com o app mostrando dois cartões grandes de instrução.
- Com a fala: a fala explica que a largura ideal da LazyVStack é a da primeira subview, e depois trata da posição de rolagem (content offset) que depende da posição estimada dos itens visíveis, citando como exemplo uma mudança de orientação no iPhone; a tela acompanha com a seta "Width" e depois a seta "Content offset", e finalmente mostra o iPhone com cartões maiores, coerente com a orientação ou proporção mencionada.

### wwdc2026_321 · folha 0004 · de 04:10 a 05:44 · código 6C80A
- Tela: diagramas abstratos do LazyVStack com contorno laranja em posições variadas (q0028 a q0030), um deles com rótulo "Content offset" e seta para baixo (q0029). Apresentador falando (q0031). Bloco de código Swift ao lado de iPhone mostrando o app "Swan" com cartões de instrução e, abaixo deles, uma seção "Showcase" com miniaturas quadradas de fotos coloridas (verde, marrom, vermelho) e nomes de pessoas abaixo ("Gabriel", "You", "Harry") em carrossel horizontal (q0032, q0033). O código evolui de `ContentView` com `Showcase()` dentro do LazyVStack (q0032) para a definição da struct `Showcase` com `ScrollView(.horizontal)` contendo `LazyHStack` e `ForEach(photos)` de `PhotoView` (q0033, q0034), com iPhone mostrando miniaturas de origami coloridas (azul, laranja) e um botão "Add Photo" no carrossel (q0034). Apresentador novamente (q0035). Por fim, diagrama abstrato de miniaturas quadradas em fileira horizontal, duas com contorno laranja e uma sem, dentro de uma barra alongada horizontal (q0036).
- Construção de interface: seção "Showcase" no app real como carrossel horizontal de fotos quadradas com cantos arredondados, cada foto com legenda de nome de usuário e data abaixo. Bloco de código com comentário `// Horizontally scrolling showcase` no topo, estrutura aninhada `ScrollView(.horizontal) { LazyHStack { ForEach(photos) { PhotoView } } }`, com trecho `LazyHStack` destacado em azul claro numa das versões. Botão "Add Photo" com ícone de câmera dentro do carrossel de fotos no app.
- Mudança entre quadros: de q0028 para q0029, aparece a seta vertical "Content offset" ao lado do mesmo diagrama abstrato. De q0032 para q0033, o código muda de mostrar `Showcase()` como uma chamada simples dentro do `ContentView` para mostrar a definição completa da struct `Showcase` com `ScrollView(.horizontal)` e `LazyHStack`; a tela do iPhone ao lado permanece mostrando o mesmo carrossel de fotos com miniaturas verdes/marrom/vermelha. De q0033 para q0034, a miniatura de fotos no carrossel do iPhone muda de fotos de pessoas para ilustrações de origami coloridas (azul, laranja) com um botão "Add Photo" adicional ao final do carrossel. De q0034 para q0035, a cena muda do iPhone para o apresentador. De q0035 para q0036, aparece um diagrama abstrato novo com três miniaturas quadradas em fileira horizontal, duas com contorno laranja (visíveis) e uma sem contorno à direita (não carregada).
- Com a fala: a fala descreve adicionar uma `Showcase` view com fotos num `ScrollView` horizontal contendo `LazyHStack`, aninhado dentro do `LazyVStack` externo; a tela mostra exatamente essa composição de código e o carrossel horizontal de fotos no app, incluindo o botão para adicionar foto mencionado implicitamente pelo fluxo do app.
### wwdc2026_321 · folha 0005 · de 05:56 a 07:03 · código A4CDC
- Tela: diagrama abstrato de carrossel horizontal (três miniaturas quadradas cinza em fileira dentro de barra arredondada), duas delas com contorno laranja e um ícone circular laranja de alerta ("!") no canto superior direito (q0037, quase repetido em q0039). Apresentador falando (q0038, q0040, q0043, q0044). Bloco de código Swift ao lado de iPhone mostrando o app "Swan" com seção "Showcase" fixada (pinned) contendo origami verde, código evoluindo de `LazyVStack(pinnedViews: [.sectionHeaders])` (q0041) para a definição de `Showcase` usando `Section { ForEach(photos) ... } header: { ... }` (q0042). Depois bloco "// Scroll effect" com código usando `.scrollTransition { effect, phase in effect.rotationEffect(...).scaleEffect(...) }` (q0045).
- Construção de interface: ícone circular laranja com ponto de exclamação no canto superior direito de dois quadros, sinalizando aviso ou problema. Seção "Showcase" no app com cabeçalho fixo (sticky header) visível no topo da tela ao rolar, foto de origami verde ocupando praticamente a largura da tela abaixo do cabeçalho "Finish" da etapa anterior. Código com comentário `// Scroll effect` e cadeia de modificadores `.scrollTransition { ... rotationEffect ... scaleEffect ... }` com sintaxe colorida (tipos em verde, funções em azul).
- Mudança entre quadros: de q0037 para q0039, o diagrama de carrossel muda a posição do contorno laranja (de envolver os dois primeiros itens da esquerda para envolver dois itens ligeiramente deslocados), com o mesmo ícone de alerta laranja no canto, indicando um problema recorrente de posicionamento. De q0041 para q0042, o código muda de uma chamada simples `Showcase()` com `LazyVStack(pinnedViews:...)` para a definição completa da struct `Showcase` usando `Section { ... } header: { ... }`; a imagem no iPhone permanece mostrando a mesma foto de origami verde com cabeçalho "Showcase" fixo no topo.
- Com a fala: a fala explica como fixar o cabeçalho da seção usando `pinnedViews` no `LazyVStack`, e a tela mostra exatamente essa propriedade no código e o cabeçalho "Showcase" fixo no topo do app; em seguida a fala introduz o `.scrollTransition` para dar efeito às fotos ao entrar e sair da tela, e a tela mostra o código desse modificador com `rotationEffect` e `scaleEffect`, junto com os ícones de alerta laranja sinalizando o problema que será demonstrado a seguir (itens que desaparecem antes da hora).

### wwdc2026_321 · folha 0006 · de 07:15 a 08:19 · código 82714
- Tela: bloco de código "// Scroll effect" completo à esquerda com iPhone à direita mostrando fotos de origami (verde, depois rosa) na seção Showcase, com ícone de alerta laranja no canto (q0046 a q0048). Depois o mesmo código mas com a linha `.scaleEffect(1 - abs(phase.value) * 0.1)` destacada e um ícone verde de confirmação (check) no canto superior direito, e iPhone mostrando as mesmas fotos de origami (verde, rosa) na seção Showcase (q0049 a q0051). Apresentador falando (q0052). Por fim, dois iPhones lado a lado mostrando o app com cartões de instrução de origami e um rótulo de texto "Scroll to Showcase button" apontando para uma área abaixo do texto do card "Fold" (q0053, q0054).
- Construção de interface: ícone de alerta laranja circular com "!" no canto superior direito nos quadros com o efeito de rotação (`rotationEffect`), substituído por ícone verde circular com marca de checagem nos quadros com o efeito de escala revisado (`scaleEffect` com `1 - abs(phase.value)`), indicando comparação visual entre abordagem problemática (laranja) e abordagem corrigida (verde). Rótulo de texto simples "Scroll to Showcase button" com linha apontando para um elemento de texto azul sublinhado "↓ Scroll to Showcase" dentro do card "Fold" no app.
- Mudança entre quadros: de q0046/q0048 para q0049/q0051, o código muda a linha de efeito de `.rotationEffect(.degrees(phase.value * 20)) .scaleEffect(1 + phase.value * 0.2)` para uma única linha `.scaleEffect(1 - abs(phase.value) * 0.1)`, e o ícone de status muda de laranja (alerta) para verde (confirmado); as fotos de origami no iPhone permanecem nas mesmas posições relativas (verde acima, rosa/vermelha abaixo). De q0053 para q0054, a tela do segundo iPhone escurece (fica com overlay escuro semitransparente) enquanto o rótulo "Scroll to Showcase button" e a seta permanecem visíveis, destacando o elemento de texto "Scroll to Showcase" dentro do card.
- Com a fala: a fala descreve o problema de usar `rotationEffect` empurrando itens para fora do frame original, fazendo-os desaparecer antes da hora (marcado com o ícone laranja de alerta), e a correção usando um `scaleEffect` simples que não desloca a posição original (marcado com o ícone verde de confirmação); a tela reproduz exatamente essa comparação antes/depois no código e nas fotos do carrossel. Em seguida, a fala apresenta um botão para rolar até a seção Showcase, e a tela mostra o rótulo indicando esse botão de texto "Scroll to Showcase" dentro do card de instrução.
### wwdc2026_321 · folha 0007 · de 08:20 a 09:40 · código 75B3E
- Tela: bloco de código "// Absolute offset" com `@State var isScrollToShowcaseVisible = false`, `ScrollView` com `.overlay(alignment: .bottom)` e `.onScrollGeometryChange(for: Bool.self) { geo in geo.contentOffset.y <= 100 }` (q0055, q0056). Apresentador falando (q0057, q0060, q0061). Código evolui para usar `.onScrollTargetVisibilityChange(idType: Step.ID.self, threshold: 0.8) { visibleIDs in isScrollToShowcaseVisible = shouldShowScrollButton(visibleIDs: visibleIDs) }`, com ícone verde de confirmação (check) no canto (q0058, q0059). Slide de transição "Layout / Subview loading / Programmatic scrolling" com "Subview loading" em destaque maior e negrito, os outros dois itens em cinza menor (q0060, q0061). Depois bloco "// Origami" com o código original simples de `ContentView` e `StepView` ao lado de iPhone com os três cartões (q0062, q0063).
- Construção de interface: comentário de seção `// Absolute offset` no topo do bloco de código indicando abordagem menos recomendada, seguido de ícone verde de checagem na versão revisada usando `onScrollTargetVisibilityChange`, mesmo padrão de cor observado na folha 0006 (laranja para abordagem com problema, verde para abordagem corrigida, embora aqui não haja ícone laranja explícito nesta folha). Slide de transição em formato de lista com hierarquia de texto: item ativo em negrito preto grande, itens adjacentes (antes e depois) em cinza claro menor, indicando posição atual dentro de um percurso de tópicos.
- Mudança entre quadros: de q0055 para q0056, o código muda de mostrar `.onScrollGeometryChange` já implementado para destacar essa mesma linha com realce azul claro de seleção, mesma estrutura. De q0058 para q0059, o código muda de mostrar `.onScrollTargetVisibilityChange` sem destaque para a mesma linha com realce azul e um ícone verde de confirmação aparecendo no canto superior direito. De q0060 para q0061, a cena alterna entre apresentador e o mesmo slide de transição "Subview loading" repetido. De q0061 para q0062, muda de slide de transição para bloco de código do app de origami original.
- Com a fala: a fala contrasta duas formas de detectar quando mostrar o botão de rolagem, usando o offset absoluto de conteúdo (`contentOffset.y`), que é impreciso porque a lazy stack estima a posição, versus usar a visibilidade relativa de subviews com `.onScrollTargetVisibilityChange`, mais confiável; a tela mostra exatamente os dois blocos de código, o primeiro sem marcação especial e o segundo com o ícone verde de aprovação. O slide de transição marca a passagem para o próximo tópico, "Subview loading", que a fala introduz a seguir revisitando o código original do app.

### wwdc2026_321 · folha 0008 · de 09:52 a 11:14 · código 27C0C
- Tela: bloco de código "// Origami" simples com `ContentView`/`LazyVStack`/`ForEach`/`StepView`, com um pequeno retângulo azul rotulado "ForEach" isolado à direita do código (q0064). Código "// Multiple subviews" mostrando `StepView` com propriedades `let step: Step` e corpo contendo `StepDiagram(...)` e `StepInstructions(...)` diretamente, sem stack encapsulando (q0065, q0066). Diagrama de blocos azuis empilhados verticalmente rotulados "StepView" (três vezes) mais um bloco menor com reticências "..." ao final, dentro de um retângulo arredondado tipo cartão de telefone (q0067). Diagrama similar mas com blocos rotulados alternando "StepDiagram" e "StepInstructions" (seis blocos) mais reticências (q0068). Apresentador falando (q0069, q0072). Código "// Dynamic number of views" com `@Environment(\.detailLevel) var detailLevel` e condicional `if step.isVisible(in: detailLevel) { VStack {...} }` (q0070, q0071), com diagrama de blocos "StepView" (três) mais reticências ao lado (q0071).
- Construção de interface: pequeno retângulo azul isolado rotulado "ForEach" funciona como anotação/rótulo apontando para um trecho específico do código sem estar dentro do bloco de código em si. Diagramas de pilha de blocos azuis retangulares com texto branco centralizado, todos do mesmo tamanho e cor, empilhados dentro de um contêiner de cantos arredondados que lembra a área de conteúdo de um telefone, terminando com um bloco menor de reticências "..." em tom mais claro de azul, indicando conteúdo adicional não mostrado.
- Mudança entre quadros: de q0064 para q0065/q0066, o comentário do código muda de "// Origami" para "// Multiple subviews", e a estrutura do `StepView` muda de encapsular tudo implicitamente para expor diretamente dois subviews (`StepDiagram` e `StepInstructions`) no corpo. De q0067 para q0068, o diagrama de blocos muda de três blocos idênticos rotulados "StepView" para seis blocos alternando "StepDiagram" e "StepInstructions" (o dobro de blocos, refletindo a divisão de cada StepView em dois subviews). De q0070 para q0071, o código adiciona a variável de ambiente `detailLevel` e a condicional `isVisible`, e o diagrama ao lado mostra blocos "StepView" com uma reticência ao final, sugerindo número variável de subviews.
- Com a fala: a fala explica que o `ForEach` resolve para um `StepView` por passo, mas que cada `StepView` pode por sua vez resolver a múltiplas subviews (`StepDiagram` e `StepInstructions`), e a lazy stack carrega essas subviews separadamente; a tela mostra exatamente essa duplicação de blocos ao passar de um diagrama para o outro. Em seguida, a fala descreve views que resolvem a um número dinâmico de subviews usando uma condicional de ambiente, e a tela mostra o código correspondente com `detailLevel` e o diagrama com reticências indicando incerteza no número de itens.
### wwdc2026_321 · folha 0009 · de 11:22 a 12:46 · código 71B59
- Tela: diagramas de blocos azuis rotulados "VStack" empilhados dentro de retângulo de telefone, um deles numerado (0, 1, 2 à esquerda dos blocos) e outro sem numeração explícita, terminando em bloco de reticências "..." (q0073, q0074, q0075). Apresentador falando (q0075). Código "// Dynamic number of views" evolui para acrescentar `@Environment(\.writingStyle) var writingStyle` junto com `detailLevel`, com a nova linha destacada em azul claro, e ícone laranja de alerta "!" no canto (q0076 a q0078). Código muda para "// Filter at the data level" usando `@Query var steps: [Step]` e `init` com `Query(filter: #Predicate<Step> { step in step.detailLevel >= detailLevel })`, trecho destacado em azul claro, ícone verde de confirmação (q0079, q0080). Código muda para "// Optional unwrapping" com `@Environment(\.apiToken) var token` e `if let token { ... }`, ícone laranja de alerta (q0081).
- Construção de interface: blocos azuis "VStack" numerados 0, 1, 2 à esquerda, sugerindo índice de posição na pilha; diferença de um diagrama para outro mostra a mesma pilha com e sem numeração, evidenciando o conceito de índice de subview. Ícones de status seguem o mesmo padrão visto nas folhas 5 e 6: laranja com "!" para abordagem arriscada/problema, verde com check para abordagem recomendada.
- Mudança entre quadros: de q0073 para q0074, o diagrama de blocos "VStack" muda de não ter números visíveis para exibir números de índice (0, 1) ao lado de cada bloco, e um terceiro item mostra "2" ao lado do bloco de reticências. De q0076 para q0077, o código ganha uma nova linha `@Environment(\.writingStyle) var writingStyle` que não existia antes, e um ícone laranja de alerta aparece no canto superior direito. De q0077 para q0078, a mesma linha nova aparece destacada com fundo azul claro e o ícone de alerta permanece. De q0079 para q0080, o trecho de código do inicializador com `Query(filter: ...)` aparece destacado em azul claro e um ícone verde de confirmação substitui o anterior. De q0080 para q0081, o comentário muda para "// Optional unwrapping" e o código muda completamente para mostrar `@Environment(\.apiToken) var token` com `if let token`, reaparecendo o ícone laranja de alerta.
- Com a fala: a fala explica que a lazy stack precisa manter StepViews antigas vivas por causa dos índices, e que mudar uma variável de ambiente não relacionada (writingStyle) pode causar atualizações desnecessárias de views fora de tela (daí o alerta laranja); em seguida recomenda filtrar no nível de dados usando `Predicate` numa `Query` (daí o ícone verde de aprovação), e observa que desembrulhar um valor opcional de ambiente tem o mesmo efeito problemático de visibilidade condicional (novo alerta laranja). A tela acompanha exatamente essas trocas de código e os ícones de status correspondentes.

### wwdc2026_321 · folha 0010 · de 12:58 a 14:25 · código E957E
- Tela: código "// Optional unwrapping" com `@Environment(NetworkClient.self) var networkClient`, ícone verde de confirmação (q0082). Apresentador falando (q0083, q0084). Diagramas abstratos de LazyVStack com contorno laranja ao redor de parte dos itens visíveis (q0085, q0086, muito semelhantes aos das folhas 0002/0003). Depois uma linha do tempo horizontal rotulada com marcadores "Frame deadline" repetidos ao longo do eixo, com um pequeno quadrado cinza e um cursor vertical azul no início (q0087), evoluindo para mostrar blocos retangulares cinza e escuro ao longo da linha do tempo, alguns rotulados "StepView 5" e "StepView 6" abaixo (q0088, q0089), e por fim um bloco vermelho destacado entre os blocos cinza na linha do tempo (q0090).
- Construção de interface: linha do tempo horizontal com marcadores verticais finos e etiquetas "Frame deadline" espaçadas regularmente ao longo do eixo, representando os limites de tempo de cada frame de renderização. Blocos retangulares de larguras variadas posicionados sobre a linha do tempo, em tons de cinza (trabalho normal) e um em vermelho (q0090, indicando problema, provavelmente trabalho que ultrapassa o prazo do frame, um "hitch"). Pequenas etiquetas de texto "StepView 5" e "StepView 6" abaixo de blocos específicos identificando a que view correspondem.
- Mudança entre quadros: de q0087 para q0088, a linha do tempo antes quase vazia (só um quadrado cinza inicial) ganha múltiplos blocos retangulares cinza e um bloco escuro maior, distribuídos entre os marcadores de "Frame deadline". De q0088 para q0089, os blocos se deslocam ligeiramente e ganham rótulos de texto "StepView 5" e "StepView 6" abaixo de blocos específicos. De q0089 para q0090, um dos blocos que antes era cinza escuro passa a ser vermelho, destacando-se visualmente dos demais blocos cinza da linha do tempo.
- Com a fala: a fala explica que, durante a rolagem, o ScrollView precisa desenhar frames a um ritmo constante, com tempo limitado até o "frame deadline", e que ultrapassar esse prazo causa um frame perdido (hitch) visível durante a rolagem; a tela mostra exatamente essa linha do tempo com os prazos de frame marcados e um bloco vermelho representando o trabalho que estourou o prazo.
### wwdc2026_321 · folha 0011 · de 14:37 a 15:52 · código 1227E
- Tela: continuação da linha do tempo com marcadores "Frame deadline", agora mostrando blocos rotulados "StepView 5 (Body)" e "StepView 5 (Layout)" abaixo de segmentos específicos (q0091, q0092), evoluindo para mais blocos com rótulos "StepView 5 (Body)", "StepView 5 (Layout)", "StepView 5", "StepView 6 (Body)", "StepView 6 (Layout)", "StepView 6" alinhados em sequência (q0093). Apresentador falando (q0094, q0095). Código "// Loading more content" com `struct Showcase` usando `@State var pager = ShowcasePager()`, `ForEach(pager.pages)`, `if !pager.atEnd { ProgressView().progressViewStyle(.circular).onAppear { pager.fetchPage() } }`, ao lado de iPhone mostrando a seção Showcase com fotos de origami e um indicador de carregamento (q0096 a q0098). Código "// onAppear" com `StepView` usando `@State var viewModel = StepViewModel()` e `.onAppear { viewModel.configure(with: id) }`, trecho destacado em azul claro, com círculo laranja sólido no canto superior direito (q0099).
- Construção de interface: linha do tempo de frames com blocos cada vez mais numerosos e detalhados, rótulos de texto pequenos abaixo identificando a fase específica do trabalho (Body, Layout) de cada view numerada, ilustrando como o trabalho de renderização é dividido em sub-etapas ao longo de múltiplos frames. No app, indicador de carregamento circular (spinner) aparece no fim do carrossel de fotos Showcase, com o trecho de código `pager.fetchPage()` destacado em azul claro. Círculo laranja sólido (sem exclamação, diferente dos ícones de alerta anteriores) aparece no canto superior direito do último código, um novo tipo de marcador.
- Mudança entre quadros: de q0091 para q0092, a linha do tempo ganha mais blocos e rótulos "StepView 5 (Body)" e "StepView 5 (Layout)" aparecem pela primeira vez abaixo de dois blocos adjacentes. De q0092 para q0093, o número de blocos rotulados na linha do tempo praticamente dobra, incluindo agora também "StepView 6 (Body)", "StepView 6 (Layout)" e "StepView 6" sozinho, mostrando três fases distintas de trabalho por view. De q0096 para q0097 para q0098, a tela alterna entre bloco de código e iPhone mostrando a mesma composição da seção Showcase com spinner de carregamento, e a linha de código `pager.fetchPage()` fica destacada em azul claro nos quadros finais. De q0098 para q0099, muda completamente de tópico de código ("Loading more content" para "onAppear"), e um marcador circular laranja sólido substitui os anteriores ícones de alerta com exclamação.
- Com a fala: a fala detalha como o trabalho de renderização de uma view (corpo e layout) pode ser dividido ao longo de múltiplos frames via prefetching, e a linha do tempo mostra exatamente essa divisão em sub-blocos rotulados "Body" e "Layout" por view. Em seguida a fala descreve o exemplo de scroll infinito usando `onAppear` num `ProgressView` para buscar mais páginas, reproduzido no código e no app com o spinner circular. Por fim, introduz um padrão a evitar, configurar tudo no `onAppear`, marcado com o círculo laranja indicando alerta.

### wwdc2026_321 · folha 0012 · de 16:04 a 17:28 · código 843AB
- Tela: código "// onAppear" com `StepViewModel` sendo criado e configurado dentro de `onAppear`, ícone laranja de alerta com "!" no canto (q0100, q0101, este com efeito de transição/desfoque sobre parte do texto). Código "// Diagram loading" mostrando `DiagramLoader` sendo usado num modificador `.task { diagram = await diagramLoader.loadDiagram(id: step.id) }` (q0102, q0103), evoluindo para mostrar a inicialização do `diagramLoader` dentro de um `init(step:)` customizado usando `_diagramLoader = State(initialValue: DiagramLoader(id: step.id))`, com a classe `@Observable class DiagramLoader` referenciada ao final (q0104). Apresentador falando (q0105, q0106). Código "// Highlighting" com `StepView` usando `@State var isHighlighted = false`, primeiro com círculo laranja sólido no canto (q0107), depois com o mesmo ícone mas agora com exclamação "!" e a linha `@State var isHighlighted = false` destacada em azul claro (q0108).
- Construção de interface: padrão consistente de ícone circular no canto superior direito dos blocos de código sinalizando o status da prática mostrada: laranja sólido sem símbolo (aviso genérico ou "atenção"), laranja com "!" (problema confirmado). Código evolui mostrando a técnica de mover a criação do "loader" do `onAppear` para o inicializador da view (`init`), like exemplificado por `DiagramLoader`.
- Mudança entre quadros: de q0100 para q0101, o código sofre uma transição visual (efeito de desfoque/crossfade) enquanto muda de mostrar a definição simples de `StepViewModel` para incluir um `init(id:)` customizado com `@State(initialValue:)`, mantendo o ícone laranja de alerta. De q0102 para q0103, pouca mudança visível no código, ambos mostram `.task` chamando `loadDiagram`. De q0103 para q0104, o código muda significativamente, acrescentando um `init(step:)` que inicializa `_diagramLoader` diretamente, e referenciando a classe `@Observable class DiagramLoader` pela primeira vez. De q0107 para q0108, o ícone no canto muda de círculo laranja sólido para círculo laranja com exclamação, e a linha `@State var isHighlighted = false` passa a ter destaque azul claro.
- Com a fala: a fala recomenda configurar a view no inicializador em vez de usar `onAppear` para evitar refazer trabalho de prefetching, e mostra isso com o exemplo de `DiagramLoader` sendo inicializado dentro do `init(step:)` da view, carregando o diagrama mais cedo; a tela reproduz exatamente essa evolução de código. Em seguida, introduz o problema de estado local (`isHighlighted`) que se perde quando a view é removida da memória ao sair da tela, marcado com o ícone de alerta laranja.
### wwdc2026_321 · folha 0013 · de 17:40 a 18:57 · código 313F6
- Tela: slide de transição "Layout / Subview loading / Programmatic scrolling", agora com "Programmatic scrolling" em destaque negrito e os dois anteriores em cinza (q0109, q0110), depois apresentador em cena real (q0111). Código "// Programmatically scroll to showcase" com `@State var scrollPosition = ScrollPosition()`, `.scrollPosition($scrollPosition)`, botão com `scrollToShowcase()` chamando `scrollPosition.scrollTo(id: "showcase-header")` dentro de `withAnimation`, ao lado de iPhone mostrando os cartões de instrução com um botão "↓ Scroll to Showcase" visível no rodapé do card "Fold" (q0112, q0113, este com diagrama abstrato ao lado com contorno laranja). Diagrama abstrato do LazyVStack com contorno laranja envolvendo blocos superiores, mais um pequeno bloco de contorno laranja isolado ao final da pilha, representando o alvo do scroll (q0114). Código "// Dynamic number of views" reaparece com ícone laranja de alerta (q0115, q0116, praticamente idênticos). Código "// Filter at the data level" reaparece com trecho destacado em azul e ícone verde de confirmação (q0117).
- Construção de interface: slide de progresso de tópicos reaparece pela terceira vez na apresentação (mesma estrutura hierárquica de texto vista nas folhas 0007 e 0013), agora com o terceiro item em foco. Botão de texto "↓ Scroll to Showcase" com seta para baixo, estilizado como link azul dentro do card, replicado do rótulo textual visto na folha 0006. Diagrama abstrato reaproveita a mesma linguagem visual de contorno laranja para indicar tanto a área visível quanto, agora, um alvo de scroll distante (fora da área visível) na parte inferior da pilha.
- Mudança entre quadros: de q0109 para q0111, a cena passa do slide de transição para o apresentador em vídeo. De q0112 para q0113, o código ganha destaque azul claro na linha `.scrollPosition($scrollPosition)` e aparece um novo diagrama abstrato ao lado da moldura do iPhone, com contorno laranja no topo da pilha. De q0113 para q0114, o iPhone desaparece e o diagrama abstrato mostra agora dois contornos laranja, um envolvendo o topo (área visível) e outro isolado próximo ao final da pilha (o alvo do scroll). De q0115 para q0116, praticamente nenhuma mudança visível no código, ambos mantêm o ícone de alerta laranja. De q0116 para q0117, o comentário do código muda de "Dynamic number of views" para "Filter at the data level", o conteúdo do código muda para usar `@Query` e `Predicate`, e o ícone muda de laranja para verde.
- Com a fala: a fala explica a rolagem programática usando `ScrollPosition` e `scrollTo(id:)`, reproduzida no código e no botão "Scroll to Showcase" visível no app; menciona que a lazy stack precisa estimar a posição de um alvo fora de tela, ilustrada pelo segundo contorno laranja isolado no diagrama abstrato. Depois repete, como recapitulação, os exemplos anteriores de filtrar visualmente (problema, alerta laranja) versus filtrar no nível de dados (correto, confirmação verde).

### wwdc2026_321 · folha 0014 · de 19:01 a 20:19 · código DE120
- Tela: apresentador falando (q0118, q0119). Código "// Don't change layout after views appear" com `StepView` usando `@State var subtitleHeight: CGFloat?`, `StepDiagram(...).frame(height: diagramHeight(subtitleHeight:))`, e `.onGeometryChange(for: CGFloat.self, of: \.size.height) { _, value in subtitleHeight = value }`, círculo cinza claro no canto (q0120), depois com ícone laranja de alerta (q0121, q0122, quase idênticos, um com leve mudança de opacidade em texto abaixo). Código muda para mostrar `StepLayout` como um `Layout` customizado substituindo a `VStack`, com ícone verde de confirmação (q0123). Apresentador falando (q0124, q0125). Slide "Next steps" com fundo de mesa de madeira e uma origami verde de pássaro (cisne) em destaque à direita (q0126).
- Construção de interface: mesmo padrão de ícones de status (cinza neutro, laranja de alerta, verde de confirmação) no canto superior direito dos blocos de código, aplicado desta vez ao problema de medir a altura de uma subview via `onGeometryChange` e usá-la para recalcular o layout de outra parte da mesma view, que a correção resolve substituindo a `VStack` padrão por um `Layout` customizado (`StepLayout`). Slide de encerramento fotográfico com origami de pássaro verde sobre mesa de madeira, similar em estilo ao slide "Next steps" do vídeo 314 mas com objeto diferente (cisne de papel em vez de ferramentas).
- Mudança entre quadros: de q0120 para q0121, o ícone no canto muda de círculo cinza neutro para laranja com exclamação, e o texto do código permanece o mesmo. De q0121 para q0122, pequena diferença de opacidade em uma parte do código (efeito de transição), mantendo o ícone de alerta laranja. De q0122 para q0123, o código muda estruturalmente: a `VStack` é substituída por `StepLayout` (um tipo customizado de `Layout`), e os modificadores de `onGeometryChange` desaparecem do corpo principal; o ícone muda para verde de confirmação. De q0125 para q0126, a cena passa do apresentador para o slide de encerramento com o cisne de origami verde.
- Com a fala: a fala descreve o problema de usar `onGeometryChange` para atualizar um estado que afeta outra parte do layout na mesma passagem, tornando a rolagem menos confiável, e recomenda usar um layout customizado (`StepLayout`) quando os primitivos de layout do SwiftUI não bastam; a tela mostra exatamente essa transição de código com os ícones de status correspondentes, seguida pelo início do encerramento do vídeo.

### wwdc2026_321 · folha 0015 · de 20:31 a 21:10 · código 3E6B7
- Tela: slide "Next steps" sobre mesa de madeira com origami verde de cisne em destaque, lista crescendo de dois para três itens: "Avoid using the absolute content size or content offset with lazy stacks" e "Filter on the data level rather than on the view level" (q0127), depois acrescido de "Set up lazy stack subviews before onAppear is called where possible" com a palavra "onAppear" formatada como código monoespaçado dentro de uma caixinha cinza (q0128). Apresentador sorrindo para encerrar (q0129), último quadro com forte filtro de cor laranja/vermelho sobreposto a toda a cena, indicando tela de créditos finais (q0130).
- Construção de interface: mesmo template de slide "Next steps" visto no vídeo 314 (fundo de mesa de madeira, título em negrito, lista de texto preto), mas aqui o objeto de destaque é um origami de cisne verde em vez de ferramentas físicas, reforçando a identidade temática do projeto de origami usado ao longo do vídeo. Termo técnico "onAppear" dentro do texto corrido aparece com formatação de código (fundo cinza claro, fonte monoespaçada), diferente do restante do texto em prosa.
- Mudança entre quadros: de q0127 para q0128, uma terceira linha de recomendação é adicionada à lista, com o termo "onAppear" destacado em formatação de código dentro da frase. De q0129 para q0130, toda a cena do apresentador recebe uma sobreposição de cor laranja/vermelha saturada, cobrindo a imagem inteira, sinalizando transição para os créditos finais do vídeo.
- Com a fala: a fala fecha o vídeo resumindo as dicas dadas ao longo da apresentação (evitar offset/tamanho absoluto, filtrar no nível de dados, configurar subviews antes do onAppear, não mudar o layout de subviews depois que aparecem) e encerra dizendo que agora "excel at creating swans"; a lista de texto no slide reproduz essas recomendações na mesma ordem em que a fala as menciona.

## Síntese visual (folhas 0001 a 0015)

- Sistema de ícones de status como linguagem visual própria: ao longo de quase todo o vídeo, um pequeno círculo no canto superior direito dos blocos de código sinaliza o veredito da prática mostrada, laranja com "!" para abordagem problemática, verde com marca de checagem para abordagem corrigida, e ocasionalmente um círculo neutro (cinza ou laranja sólido sem símbolo) antes da conclusão. Esse padrão aparece nas folhas 0005, 0006, 0007, 0009, 0011, 0012, 0013 e 0014, formando uma trilha visual consistente de "antes / depois" que a fala explica em palavras mas que só a imagem materializa como código de cor reutilizado sistematicamente.
- Diagrama abstrato reaproveitado como "notação" da lazy stack: da folha 0002 até a 0013, o mesmo desenho esquemático (barras cinza dentro de um retângulo vertical alongado, com contorno laranja marcando a região "visível") é reciclado para ilustrar conceitos diferentes, área visível versus estimada, largura, content offset, flow de novos itens, alvo de scroll fora de tela. É uma notação visual própria do vídeo, não mencionada como tal na fala, que se sofistica progressivamente (ganha rótulos, setas de medida, numeração de índice) conforme o vídeo avança.
- Linha do tempo de frame deadline: as folhas 0010 e 0011 introduzem uma representação nova, um eixo horizontal com marcadores "Frame deadline" e blocos de trabalho coloridos (cinza para normal, vermelho para estouro de prazo), que evolui para mostrar a subdivisão do trabalho de uma view em fases "Body" e "Layout" distribuídas entre frames. Essa granularidade de sub-etapas por frame não é verbalizada em detalhe na fala, apenas sugerida; a imagem é quem mostra exatamente como o trabalho se distribui.
- Consistência do app de demonstração: o aplicativo fictício "Swan" (instruções de origami) aparece em praticamente toda folha que mostra iPhone, com o mesmo estilo visual (fundo azul claro nas ilustrações, título em negrito, texto explicativo cinza) do início ao fim, incluindo a evolução incremental da mesma tela, cartões de passo, depois carrossel de fotos "Showcase", depois seção fixada, depois botão de scroll, permitindo ver a app crescer em complexidade junto com o código explicado.
- Slides de progresso de tópicos como marcador de estrutura: o slide de três itens ("Layout", "Subview loading", "Programmatic scrolling") reaparece pelo menos três vezes (folhas 0007, 0011, 0013) com o item atual sempre em negrito preto grande e os demais em cinza claro menor, funcionando como um índice de navegação visual que pontua as três grandes seções do vídeo, reforço estrutural que a imagem comunica de forma mais explícita do que a transição falada.
- Fechamento temático: o slide final "Next steps" (folha 0015) usa o mesmo template do slide equivalente no vídeo 314 (mesa de madeira, objeto físico de destaque, lista de recomendações), mas substitui as ferramentas genéricas por um origami de cisne verde, callback visual direto ao projeto de exemplo do vídeo, reforçando consistência de identidade entre a abertura, o corpo e o encerramento.
