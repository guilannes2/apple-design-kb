### wwdc2026_322 · folha 0001 · de 00:00 a 01:22 · código BEA1D
- Tela: abertura "WWDC26" em fundo laranja (q0001), depois apresentador falando sentado a uma mesa de madeira com laptop verde ao fundo (q0002, q0003, q0006), uma tela branca vazia de transição (q0004), ilustração de tubulação metálica tipo encanamento com um medidor circular rotulado "BYTES" e uma válvula vermelha (q0007, q0008), e um quadro de transição de seção com o texto "Design breakdown" sobre fundo vermelho escuro ao lado do apresentador (q0009).
- Construção de interface: a peça central visual é a metáfora de pipeline construída como tubos metálicos conectados por juntas, com um medidor de fluxo em vidro/cromado no meio do sistema de canos e uma válvula de controle vermelha. É uma ilustração 3D renderizada, não uma tela de app.
- Mudança entre quadros: de q0007 para q0008 os canos e o medidor permanecem no mesmo enquadramento, sem alteração visível de posição (possível variação mínima de câmera). De q0006 para q0007 a cena muda do apresentador para a ilustração de tubulação.
- Com a fala: enquanto a fala descreve a SwiftUI como um "pipeline" de dados que passa por uma série de tubos padrão que transformam a informação, a tela mostra literalmente a ilustração de encanamento com medidor e válvula, reforçando a metáfora antes de entrar no roteiro do design breakdown.

### wwdc2026_322 · folha 0002 · de 01:34 a 02:42 · código FF95B
- Tela: slide de agenda com três itens em lista ("Design breakdown", "Advanced graphics", "Creative pipeline") ao lado do apresentador (q0010); tela branca vazia (q0011); mockup de iPhone mostrando um app de detalhe de sessão de WWDC com texto de descrição (q0012); o mesmo mockup com um efeito translúcido tipo vidro fosco cobrindo o bloco de texto, com controles de reprodução na base (q0013); diagramas de pipeline em cano metálico com uma lente circular tipo lupa acoplada ao cano exibindo um preview em miniatura da tela do app, rotulada "Original UI" (q0014, q0015); diagrama de pipeline ramificando em Y a partir de "Original UI" em três saídas rotuladas "Cover art", "Playback info" e "Transcript", cada uma com um ícone circular de preview (imagem colorida, símbolo de play/pause, bloco de texto) (q0016 a q0018).
- Construção de interface: hierarquia tipográfica no slide de agenda por peso e cor (item ativo em branco bold, itens anteriores em cinza esmaecido, controlados por qual etapa está sendo descrita). No mockup do app, camada de material translúcido (glassmorphism) sobrepõe o texto de fundo, com barra de controles fixa na base da tela. Nos diagramas de pipeline, cada nó de dado é representado por um círculo tipo lente/medidor acoplado ao cano, com um mini preview do estado da informação naquele ponto; a ramificação de um cano único em múltiplos é desenhada como bifurcação em Y física.
- Mudança entre quadros: de q0012 para q0013 o bloco de texto do app ganha uma camada de desfoque translúcido sobre parte do conteúdo, sem alterar o restante do layout. De q0014 para q0015 a lente de preview se desloca ao longo do cano, mesmo conteúdo "Original UI". De q0016 para q0017 para q0018 o ícone de preview no ramo "Cover art" muda de um retângulo genérico para uma imagem colorida em gradiente, indicando progressão da transformação nesse ramo.
- Com a fala: enquanto a fala explica que a interface existente já contém todos os dados necessários (capa, informação de reprodução, texto da transcrição) e que a questão é como transformar esses dados pelo pipeline, a tela mostra exatamente essa decomposição: a UI original se ramificando em três fluxos de dados distintos, cada um com seu próprio preview.

### wwdc2026_322 · folha 0003 · de 02:46 a 03:38 · código CA29C
- Tela: sequência de diagramas de pipeline em cano metálico com caixas retangulares no meio do cano nomeando a transformação aplicada e lentes circulares nas pontas mostrando o estado do dado antes e depois: "Shader effects" transformando "Cover art" em "Visualizer" (q0019); "Timeline input" cruzando com o fluxo de "Current time" para gerar "Dynamic visual" (q0020, q0021); "View overlay" e depois "Auto scroll" transformando "Transcript"/"Timestamps" em "Scrolling text", com o ramo de "Current time" se conectando ao fluxo (q0022 a q0024); o ramo de "Dynamic visual" convergindo para um ícone final rotulado "Final UI" em forma de iPhone (q0025); mockups de iPhone mostrando a tela final renderizada, com fundo em gradiente colorido (rosa, azul, laranja) e texto sobreposto, emoldurados por uma vinheta circular escura tipo lente (q0026, q0027).
- Construção de interface: o vocabulário visual do pipeline é consistente: caixa retangular rotulada = etapa de transformação (modificador/API), círculo/lente = estado do dado (visualizado como miniatura), linha de cano = fluxo, junção em X ou Y = merge ou branch de dois fluxos. O preview final da tela usa um efeito de vinheta circular escura ao redor do mockup do iPhone, como se estivesse sendo visto através de uma lente.
- Mudança entre quadros: de q0020 para q0021 o fluxo de "Current time" se conecta ao ramo que já contém "Timeline input" e "Visualizer", formando uma junção onde dois canos se cruzam em X, produzindo "Dynamic visual". De q0022 para q0023 para q0024 o pipeline de baixo evolui: "Transcript" e "Timestamps" passam por "View overlay" e depois um novo estágio "Auto scroll" aparece à direita, conectando-se ao fluxo de "Current time" vindo de cima, e o rótulo final muda de "Timestamps" para "Scrolling text". De q0026 para q0027 a vinheta circular escura ao redor do mockup do iPhone fica mais fechada/proeminente, como uma transição de zoom ou corte.
- Com a fala: a fala descreve a conexão dos pipes de visualização de capa e transcrição sincronizada por tempo, e a junção final dos dois fluxos paralelos; a tela mostra exatamente essa arquitetura em diagrama, com os rótulos de API/estágio nomeados (Shader effects, Timeline input, View overlay, Auto scroll) que não são ditos literalmente na fala nesse trecho, mas ilustram os nomes das etapas do pipeline.

### wwdc2026_322 · folha 0004 · de 03:49 a 04:53 · código 5193C
- Tela: slides de lista ao lado do apresentador detalhando os componentes de "Advanced graphics": primeiro com três itens ("Full-screen cover art", "Shader effect", "Time-driven animation") (q0029, q0030), depois expandido para cinco itens incluindo "Time-synced scroll view" e "Floating-view attachment" (q0033); telas com código Swift ao lado de mockup de iPhone: `Image("CoverArt")` mostrando uma imagem colorida nítida em gradiente (q0031), depois `Image("CoverArt").blur(radius: 30)` com destaque de sintaxe roxo no parâmetro, mostrando a mesma imagem desfocada (q0032); mockup de iPhone com uma tela de app SwiftUI genérica mostrando cards de conteúdo e o ícone do logo do Swift (q0035), com um círculo de zoom/lupa ampliando esse ícone e rotulando-o "Vector" ao lado (q0036); uma tela branca vazia de transição (q0034).
- Construção de interface: hierarquia por peso e cor nos slides de lista (item em foco branco bold, os demais cinza esmaecido), mesmo padrão da folha 0002. Bloco de código Swift em fonte monoespaçada com destaque de sintaxe (comentário em cinza, string em vermelho/laranja, nome de parâmetro em roxo) posicionado ao lado do mockup de iPhone que reflete o resultado do código em tempo real. Uso de lupa/círculo de zoom para apontar um elemento específico da tela (o ícone) e nomeá-lo como "Vector", indicando que a discussão passa de bitmap para forma vetorial.
- Mudança entre quadros: de q0031 para q0032 o código ganha a linha `.blur(radius: 30)` e a imagem no mockup correspondente passa de nítida para visivelmente desfocada, mesma composição de cores. De q0029/q0030 para q0033 a lista de itens cresce de três para cinco, com os dois primeiros grupos (cover art, shader, animação) passando a cinza esmaecido e mantendo apenas "Full-screen cover art" em destaque bold branco.
- Com a fala: a fala descreve a aplicação de um modificador `.blur` na imagem de capa "para que não compita" com a transcrição antes de aplicar o shader, e depois explica que um ícone vetorial é rasterizado pela GPU em pixels; a tela mostra exatamente esse código de blur sendo adicionado e, na sequência, o ícone do Swift sendo apontado e rotulado "Vector" como ponto de partida da explicação sobre rasterização.
### wwdc2026_322 · folha 0005 · de 05:05 a 06:21 · código EF69D
- Tela: mockup de iPhone com tela de app SwiftUI genérica, com um círculo de lupa apontando o ícone do Swift e ampliando-o até mostrar uma grade de pixels quadriculados em tons de azul, rotulada "Pixel" (q0037, q0039, q0040); apresentador falando (q0038); diagramas de grade de pixels de "Input" e "Output" lado a lado com um bloco de comentário `// Shader effects` e três assinaturas de função em código listadas acima (`colorEffectFunction`, `distortionEffectFunction`, `layerEffectFunction`), com a primeira linha destacada em azul claro (q0041 a q0045); nos quadros seguintes, marcadores retangulares amarelos "Position: (8,4)" no input e "Color: (0.92, 0.92, 0.92, 1.0)" no output conectados por uma seta (q0043, q0044), evoluindo para dois marcadores de posição diferentes ligados por setas cruzadas "Position: (8,4)" e "Position: (8,0)" (q0045).
- Construção de interface: grade de pixels desenhada como quadrados de cor lado a lado (estilo mapa de calor azul) representando "Input" e "Output" de uma função de shader, com anotações de código sobrepostas mostrando as três assinaturas de shader effect da API SwiftUI. Uso de caixas de rótulo amarelas com setas para anotar valores de posição e cor pixel a pixel, ligando o pixel de entrada ao de saída.
- Mudança entre quadros: de q0041 para q0042 o destaque de sintaxe (fundo azul claro) se move da primeira linha (`colorEffectFunction`) para a segunda linha de assinatura (`distortionEffectFunction`) no bloco de código. De q0043 para q0044 o marcador de posição/cor permanece igual, mas o grid de output muda de padrão de cinza. De q0044 para q0045 os rótulos amarelos deixam de mostrar "Color:" e passam a mostrar duas posições diferentes ("Position: (8,4)" e "Position: (8,0)") ligadas por setas em ambas as direções, indicando a mudança de comportamento entre colorEffect e distortionEffect.
- Com a fala: a fala explica que um shader roda em paralelo por pixel, sem conhecimento dos vizinhos, e descreve as três assinaturas de shader effect do SwiftUI (colorEffect, que transforma cor por posição; distortionEffect, que pega uma posição existente e retorna nova posição sem envolver cor). A tela mostra exatamente essas assinaturas de código e a diferença visual entre anotar cor versus anotar posição nos diagramas de pixel.

### wwdc2026_322 · folha 0006 · de 06:28 a 07:45 · código 3F663
- Tela: apresentador falando (q0046); diagramas de grade de pixels "Input"/"Output" com a linha `layerEffectFunction` destacada em azul claro, com setas ligando posições de pixel diferentes (q0047, q0048); nos quadros seguintes, um quadrado azul maior destacado no input ("Sampled: (8±2,4±2)") ligado por seta a um pixel específico no output ("Position: (8,4)") (q0049, q0050); código Swift `GeometryReader` com `.layerEffect(ShaderLibrary.backgroundWarp(), maxSampleOffset: .zero)` e código Metal correspondente `backgroundWarp` que apenas retorna `layer.sample(position)`, ao lado de mockup de iPhone com imagem de capa em gradiente colorido, sem distorção (q0051 a q0054).
- Construção de interface: mesmo vocabulário de grade de pixels com marcadores amarelos de posição, mas agora um quadrado maior delimitando uma região amostrada ("Sampled") em vez de um único pixel, indicando que o layerEffect pode ler uma vizinhança de pixels. Par de blocos de código lado a lado rotulados "// SwiftUI, shader" e "// Metal, shader", com sintaxe colorida (roxo para tipos, verde para nomes de função) e mockup de iPhone mostrando o resultado renderizado ao lado do código.
- Mudança entre quadros: de q0047 para q0048 o destaque de sintaxe avança para a terceira assinatura (`layerEffectFunction`). De q0049 para q0050 a região "Sampled" amostrada no input permanece do mesmo tamanho, mas a posição de saída destacada muda de lugar no grid de output. De q0051 para q0052 a q0054 o layout se repete de forma idêntica, com o código e o mockup de capa colorida sem distorção mantidos estáveis (nenhuma mudança visível entre esses quadros).
- Com a fala: a fala descreve o layerEffect como o mais flexível, permitindo amostrar pixels adjacentes ou a região inteira da camada, e mostra a criação do modificador `.layerEffect` com a função `backgroundWarp` que inicialmente apenas devolve a mesma imagem sem alteração; a tela reflete exatamente esse código inicial e o resultado idêntico ao original antes de qualquer distorção.

### wwdc2026_322 · folha 0007 · de 07:57 a 09:19 · código 05677
- Tela: sequência de blocos de código lado a lado com mockup de iPhone, progredindo de "// SwiftUI, shader with offset" e "// Metal, shader with offset" (parâmetro `float2 offset` adicionado à função Metal, retorno `layer.sample(position + offset)`) (q0055 a q0058), para a chamada do shader ganhando um argumento `.float2(.init(x: proxy.size.width, y: 0))` depois alterado para `.float2(.init(x: 0, y: 0))` (q0056, q0057, q0059), até a introdução de "// SwiftUI, shader with noise" e "// Metal, shader with noise" com o argumento `.image(Image("NoiseTexture"))` adicionado à chamada do shader (q0060, q0061); apresentador falando (q0058); diagrama final de código Metal com parâmetro `texture2d<half> noiseTex` e a linha `float2 uv = position / size;` destacada (q0062, q0063).
- Construção de interface: mesmo par de blocos "SwiftUI" / "Metal" lado a lado com mockup de iPhone mostrando o resultado; a imagem de capa no mockup permanece visualmente idêntica (gradiente laranja/rosa/azul) em todos os quadros dessa folha, sem distorção visível ainda. Destaque de sintaxe azul claro usado para indicar a linha de código sendo adicionada ou explicada no momento.
- Mudança entre quadros: de q0055 para q0056 o código Swift ganha a linha `.float2(.init(x: proxy.size.width, y: 0))` dentro da chamada de `backgroundWarp`. De q0056 para q0057 o valor do argumento muda de `x: proxy.size.width` para `x: 0`. De q0059 para q0060 o código passa de "shader with offset" para "shader with noise", trocando o argumento offset por `.image(Image("NoiseTexture"))`. De q0061 para q0062 o foco de código muda do lado SwiftUI para o lado Metal, revelando a assinatura completa da função com `texture2d<half> noiseTex` como parâmetro.
- Com a fala: a fala explica o aumento e diminuição do valor de offset e a limitação de um padrão fixo e uniforme, levando à necessidade de uma NoiseTexture para variação orgânica por pixel; a tela mostra exatamente essa evolução de código, do parâmetro offset fixo para o parâmetro de textura de ruído, embora a imagem no mockup ainda não mostre visualmente a distorção nesses quadros específicos.

### wwdc2026_322 · folha 0008 · de 09:31 a 10:53 · código CD6E7
- Tela: diagramas explicando os canais RGB de uma textura de ruído: três quadrados de imagem de ruído rotulados R (vermelho), G (verde), B (azul, aparece sólido escuro) com um ponto branco circular marcando a posição de amostragem (uv) e uma barra de escala de 0 a 1 abaixo de cada canal, ao lado do mockup de iPhone com a imagem de capa ainda não distorcida (q0064, q0065, q0067); apresentador falando (q0066); código Metal completo da função `backgroundWarp` com `sampler` de modo repeat, cálculo de `uv`, amostragem do ruído (`noiseTex.sample`) e cálculo de offset a partir dos canais R e G, com a imagem no mockup agora visivelmente distorcida em manchas onduladas (q0068, q0069); código Metal de "domain warping" com uma segunda amostragem de ruído deslocada pela primeira (`n = noiseTex.sample(s, uv + q)`), com a imagem no mockup evoluindo para manchas mais orgânicas e fluidas em cores vivas (laranja, rosa, azul, roxo) (q0070 a q0072).
- Construção de interface: uso de três miniaturas de textura de ruído coloridas por canal (vermelho, verde, azul) com ponto de amostragem marcado e barra de valor de 0 a 1, uma forma didática de visualizar dados de textura antes de aplicá-los no código. Bloco único de código Metal completo (sem par lado a lado com SwiftUI nesta folha), com comentário "// domain warping" separando a etapa extra de amostragem dupla. O resultado visual no mockup evolui de gradiente limpo para padrão de manchas orgânicas tipo tinta fluida.
- Mudança entre quadros: de q0064 para q0065 o ponto de amostragem branco se move para uma posição diferente em cada canal (uv muda de (0.5,0.5) para (0.8,0.2)), e o mockup ao lado mostra o mesmo gradiente sem distorção visível ainda. De q0067 para q0068 o layout muda de três canais RGB para o bloco único de código Metal completo, e a imagem do mockup passa a mostrar distorção em manchas onduladas pela primeira vez. De q0070 para q0071 para q0072 a imagem no mockup fica progressivamente mais distorcida e colorida, com manchas mais largas e fluidas, culminando em um padrão quase abstrato de cores contrastantes.
- Com a fala: a fala descreve a leitura dos canais vermelho e verde do NoiseTexture como par de valores para offset em X e Y, a criação de um sampler em modo repeat, e depois a técnica de "domain warping" (amostrar o ruído duas vezes, a segunda deslocada pela primeira) para obter manchas orgânicas fluidas; a tela mostra exatamente essa evolução de código e o resultado visual cada vez mais orgânico no mockup do iPhone.
### wwdc2026_322 · folha 0009 · de 11:05 a 12:15 · código 3AE61
- Tela: código Metal de "domain warping" com mockup de iPhone mostrando a imagem de capa distorcida em manchas coloridas (q0073); slides de lista "Advanced graphics" com o item "Shader effect" destacado em bold (q0074), depois "Time-driven animation" destacado (q0079) e "Time-synced scroll view" destacado (q0080), sempre ao lado do apresentador; código SwiftUI "// SwiftUI, static shader" com `CoverArtView().layerEffect(...)` ao lado do mockup com imagem distorcida estática (q0075, q0076); código "// SwiftUI, animated shader" introduzindo `@State private var startDate = Date.now` e `TimelineView(.animation)` com `elapsed` calculado e passado como `.float(elapsed)` ao shader (q0077, q0078); código "// Basic transcript" com `ScrollView { LazyVStack(...) { ForEach(sampleTranscript) { Text(line.text).font(.title).fontWeight(.bold) } } }` ao lado de mockup de iPhone mostrando uma lista de texto de transcrição em negrito uniforme (q0081).
- Construção de interface: transição de shader estático para animado marcada pela adição de `TimelineView(.animation)` e uma variável de estado de tempo `elapsed`, mesmo padrão de bloco de código único com mockup ao lado usado nas folhas anteriores. Novo bloco de transcrição usa `ScrollView` com `LazyVStack` e `ForEach`, texto em fonte título bold uniforme para todas as linhas (ainda sem diferenciação de linha atual).
- Mudança entre quadros: de q0075/q0076 para q0077/q0078 o código ganha a declaração de estado `startDate` e o wrapper `TimelineView(.animation) { timeline in ... }`, e a chamada do shader ganha o argumento `.float(elapsed)`; a imagem no mockup permanece com o mesmo padrão de manchas coloridas (estática nas capturas, mas o código agora a anima). De q0074 para q0079 para q0080 o item em destaque bold na lista de "Advanced graphics" avança de "Shader effect" para "Time-driven animation" para "Time-synced scroll view".
- Com a fala: a fala explica que shaders são sem estado e que é preciso passar um valor que muda com o tempo via TimelineView para animar, e depois introduz a view de transcrição com Text views em LazyVStack dentro de ScrollView; a tela mostra exatamente essa adição de TimelineView e startDate ao shader, e o código básico de transcrição descrito.

### wwdc2026_322 · folha 0010 · de 12:27 a 14:01 · código C61DD
- Tela: código "// Basic transcript" ao lado do mockup mostrando todas as linhas de transcrição em bold uniforme (q0082); código "// Time-synced transcript" introduzindo `@State private var playback = PlaybackState()`, `ScrollViewReader`, `.transcriptLineStyle(isCurrent: line.id == playback.currentLineIndex)` e `.onChange(of: playback.currentLineIndex) { scrollProxy.scrollTo(i, anchor: .center) }`, com o mockup agora mostrando apenas a primeira linha em bold e as demais em cinza claro (q0083, q0084); diagramas de caixas de texto com timestamps sobrepostos em cinza escuro acima de cada linha ("00:00 - 00:08", "00:08 - 00:14", "00:14 - 00:38") ao lado de blocos de texto de transcrição individuais (q0085, q0086); diagrama de duas caixas retangulares laranja rotuladas "Container view" e "Sub view" alinhadas uma abaixo da outra (q0087); diagramas de alinhamento com linhas pontilhadas cruzando o centro de "Container view" e "Sub view", rotuladas ".center" em ambos os lados (q0088, q0089); código "// Align sub view in container view" com `Text(line.text) // Container View` e `.overlay { Text(line.formattedTimestamp) // Sub View }`, ao lado de um diagrama 3D isométrico de dois planos rotulados "Container view" e "Sub view" conectados por linhas pontilhadas (q0090).
- Construção de interface: transição de texto bold uniforme para hierarquia com apenas a linha atual em bold preto e as demais em cinza claro (fade), controlada por `isCurrent`. Uso de `ScrollViewReader` com `scrollTo(anchor: .center)` disparado por `onChange`. Diagrama didático de alinhamento usa duas caixas retangulares laranja com um ponto central e linha pontilhada cruzando ambas, rotulado ".center" nos dois lados, para ilustrar o conceito de "pino" de alinhamento entre view container e subview.
- Mudança entre quadros: de q0082 para q0083/q0084 o texto no mockup passa de todas as linhas em bold para apenas a primeira linha em bold preto e as demais esmaecidas em cinza, refletindo a adição de `transcriptLineStyle(isCurrent:)` no código. De q0085 para q0086 aparecem caixas de timestamp em cinza escuro acima de cada bloco de texto, numerando os intervalos de tempo de cada linha. De q0087 para q0088 o diagrama de duas caixas ganha uma linha pontilhada central conectando os dois centros com rótulos ".center" em ambos os lados. De q0089 para q0090 o diagrama passa de retângulos planos 2D para uma representação 3D isométrica dos dois planos "Container view" e "Sub view".
- Com a fala: a fala descreve o uso do timestamp de reprodução para determinar a linha atual (em negrito, o resto esmaecido), o scroll automático centralizado via onChange, e depois introduz o conceito de alinhamento como um "pino" que atravessa container e subview no ponto de alinhamento padrão (center); a tela mostra exatamente essa transição de estilo de texto e o diagrama do pino de alinhamento central.

### wwdc2026_322 · folha 0011 · de 14:13 a 15:33 · código E363D
- Tela: sequência de diagramas 3D isométricos de "Container view" e "Sub view" com linhas pontilhadas e pontos coloridos marcando pontos de alinhamento, evoluindo de um rótulo genérico de pino central para rótulos ".top"/".bottom" em laranja próximos ao "Sub view" (q0091 a q0093); apresentador falando (q0094); código "// Align sub view in container view using the bottom leading alignment" com `.overlay(alignment: .bottomLeading)` e depois `.alignmentGuide(.bottom) { $0[.bottom] }` mudando para `.alignmentGuide(.bottom) { $0[.top] }`, sempre ao lado do mesmo diagrama 3D isométrico com rótulos ".top" e ".bottom" (q0095 a q0099).
- Construção de interface: diagrama 3D isométrico consistente ao longo da folha, com dois planos paralelos (container e subview) conectados por linhas pontilhadas e pontos de ancoragem; rótulos de alinhamento (.top, .bottom) aparecem como pequenas etiquetas laranja fixadas nos pontos relevantes do subview. Código com `.alignmentGuide` demonstrando a customização do ponto de alinhamento por closure retornando `$0[.bottom]` ou `$0[.top]`.
- Mudança entre quadros: de q0091 para q0092 o diagrama gira/reposiciona levemente mas mantém a mesma estrutura de dois planos conectados. De q0092 para q0093 aparecem pela primeira vez os rótulos ".top" e ".bottom" em caixas laranja junto ao "Sub view", e o "Container view" fica esmaecido. De q0095 para q0096 o valor dentro do closure `.alignmentGuide(.bottom)` muda de `$0[.bottom]` para `$0[.top]`, e o rótulo no diagrama muda de posição de ".bottom" para ".top" relativo ao ponto de ancoragem do subview. De q0096 para q0097 o código se mantém com `$0[.top]` e o diagrama se estabiliza na nova configuração.
- Com a fala: a fala explica a mudança do alinhamento do overlay para `.bottomLeading` e depois a customização via `alignmentGuide` para que o "bottom" do subview aponte para seu próprio "top" (fazendo a borda superior do subview tocar a borda inferior do container); a tela mostra exatamente essa progressão de código e a mudança de rótulo no diagrama de ".bottom" para ".top" como consequência direta.

### wwdc2026_322 · folha 0012 · de 15:45 a 16:41 · código CB6FE
- Tela: mockups de iPhone mostrando a tela final do app com capa em gradiente colorido (rosa, laranja, azul) como fundo, transcrição sobreposta com uma linha em destaque bold com timestamp em caixa vermelha, e controles de reprodução na base (q0100 a q0103); composição com mockups de iPhone e iPad lado a lado mostrando a mesma tela em tamanhos diferentes, com um texto de legenda técnica parcialmente visível ao fundo desfocado (q0104); ilustração de pipeline metálico com uma lupa circular acoplada mostrando o mockup final do iPhone (q0105); diagrama de pipeline completo em cano metálico ramificando de "Original UI" em múltiplos ramos (imagem, play/pause, texto) que convergem de volta em um ícone final (q0106); slide de título "Design breakdown / Advanced graphics / Creative pipeline" com o item "Creative pipeline" em destaque bold branco, ao lado do apresentador (q0107); tela branca vazia (q0108).
- Construção de interface: tela final do app real combina fundo de shader animado em gradiente, overlay de texto de transcrição com hierarquia de opacidade (linha atual em preto bold, demais cinza claro) e uma etiqueta de timestamp em destaque vermelho/laranja na linha atual, com controles de reprodução fixos na base em estilo translúcido escuro. O diagrama de pipeline final reconecta todos os ramos vistos anteriormente (cover art, playback info, transcript) em uma única rede visual, mostrando a convergência de múltiplas transformações em uma UI final.
- Mudança entre quadros: de q0100 para q0101 para q0102 a linha de transcrição em destaque avança de posição (a etiqueta de timestamp vermelha muda de linha, indicando o scroll sincronizado com o tempo de reprodução). De q0103 para q0104 a composição se expande de um único mockup de iPhone para um conjunto com iPad e outro dispositivo, mostrando a mesma interface em múltiplos tamanhos de tela.
- Com a fala: a fala resume o resultado final, "an animated background driven by a shader and time, a transcript that scrolls in sync with playback, and a floating timestamp positioned with alignment guides... and it works across Apple devices", e a tela mostra exatamente esse resultado funcionando no mockup do iPhone com scroll sincronizado, seguido da mesma tela replicada em iPad, confirmando a afirmação de "across Apple devices".
### wwdc2026_322 · folha 0013 · de 16:53 a 17:47 · código A0C87
- Tela: ilustração de tubulação metálica ramificada em Y com medidor circular "BYTES", sobre fundo quadriculado claro, com o texto "Creative pipeline" em destaque tipográfico grande (q0109, q0110); apresentador falando (q0111); slides "Next steps" sobre uma mesa de madeira com uma chave de cano (ferramenta física real, vermelha e cinza) e um iPhone verde ao lado, com itens de lista aparecendo progressivamente: primeiro só o título (q0112), depois "Download the sample and experiment" e "Check out documentation" (q0113), depois acrescido de "Use creative pipelines" (q0114); apresentador em pé mexendo em um dispositivo na mesa (q0115, q0116).
- Construção de interface: reaparição da ilustração de encanamento metálico com medidor "BYTES" vista na folha 0001, agora rotulada explicitamente "Creative pipeline" em tipografia grande bold preta sobre fundo de grade clara, fechando a metáfora visual do vídeo. Os slides finais "Next steps" usam uma composição fotográfica com objetos físicos (chave inglesa de cano de verdade, iPhone) sobre madeira, reforçando literalmente a metáfora de "pipeline" com uma ferramenta de encanador real, e lista de itens revelados um a um.
- Mudança entre quadros: de q0112 para q0113 para q0114 a lista de itens sob "Next steps" cresce progressivamente de zero para três itens, mantendo o mesmo enquadramento de mesa, chave de cano e iPhone. De q0109 para q0110 a câmera se desloca ligeiramente sobre a mesma ilustração de tubulação, revelando o texto "Creative pipeline" que não estava visível no quadro anterior.
- Com a fala: a fala fecha explicando que a mesma API pode ser recombinada de formas diferentes (dados de giroscópio em vez de áudio, um shader de ondulação em vez de torção) e convida a baixar o projeto de exemplo e experimentar; a tela mostra a ilustração de pipeline rotulada e os passos seguintes (baixar amostra, ver documentação, usar pipelines criativos), reforçando visualmente o convite dito na fala. Os dois últimos quadros (apresentador mexendo num dispositivo) não têm fala transcrita nesta folha, correspondem ao encerramento silencioso do vídeo.

## Síntese visual (folhas 0001 a 0013)

- Metáfora central de "pipeline" construída como ilustração 3D de tubulação metálica de encanador, com medidor de fluxo "BYTES" e válvula vermelha, usada na abertura (folha 0001, q0007-q0008) e retomada no encerramento com o rótulo explícito "Creative pipeline" (folha 0013, q0109-q0110); a fala usa a mesma metáfora verbalmente, mas a escolha de desenhar uma tubulação de encanamento físico (não um diagrama abstrato de setas) e reforçar com uma chave de cano real na cena final (folha 0013, q0112-q0114) é um reforço visual que vai além do que a fala descreve.
- Diagramas de pipeline como sistema de notação consistente ao longo do vídeo: círculos/lentes = estado do dado (com mini-preview), caixas retangulares no meio do cano = nome da transformação/API aplicada, ramificação em Y = branch, cruzamento em X = merge de dois fluxos (folhas 0002 e 0003, q0014-q0025); esse vocabulário visual nomeia etapas (Shader effects, Timeline input, View overlay, Auto scroll) que não são necessariamente citadas literalmente na fala no mesmo instante, funcionando como legenda técnica adicional à narração.
- Padrão didático de par de blocos de código lado a lado ("// SwiftUI" e "// Metal") com um mockup de iPhone mostrando o resultado renderizado ao lado, repetido consistentemente da folha 0005 à folha 0009 conforme o shader evolui (colorEffect/distortionEffect/layerEffect na folha 0005; layerEffect com backgroundWarp nas folhas 0006-0007; noise e domain warping nas folhas 0007-0009); a progressão do código é visível quadro a quadro com trechos novos destacados em azul claro, permitindo rastrear exatamente qual linha foi adicionada em cada mudança, informação mais granular do que a fala sozinha transmite.
- Visualização didática de grade de pixels em tons de azul com marcadores de posição e cor anotados (retângulos amarelos "Position:" e "Color:") para explicar o funcionamento pixel a pixel dos shader effects (folhas 0005-0006), incluindo a diferença visual entre amostrar um único pixel (colorEffect/distortionEffect) e uma vizinhança de pixels (layerEffect, região "Sampled" maior).
- Decomposição de uma textura de ruído em canais RGB individuais, cada um mostrado como imagem separada com ponto de amostragem e barra de valor 0 a 1 (folha 0008, q0064-q0067), uma visualização que não tem equivalente em palavras na fala além de "the red and green channels are interesting", a tela é quem efetivamente demonstra o conceito.
- Diagramas de alinhamento em duas fases: primeiro planos retangulares 2D com linha pontilhada central e rótulo ".center" (folha 0010, q0087-q0090), depois diagramas 3D isométricos com pontos de ancoragem coloridos e rótulos ".top"/".bottom" (folha 0011, q0091-q0099), sincronizados com a evolução do código de `.overlay(alignment:)` para `.alignmentGuide`; a fala usa a metáfora do "pino que atravessa as views" e o diagrama a materializa literalmente com um ponto de conexão entre os dois planos.
- Hierarquia tipográfica consistente nos slides de agenda e de resumo de tópicos (branco bold = item ativo, cinza esmaecido = itens passados ou futuros), usada tanto na introdução (folha 0002, q0010) quanto no meio do vídeo para marcar progresso dentro da lista "Advanced graphics" (folhas 0004, 0009, 0012).
- Evolução visual do mockup do app do apresentador ao longo do vídeo: da tela estática de texto simples (folha 0002) para a capa borrada (folha 0004), para a capa com shader estático (folha 0009), para a capa animada com transcrição sincronizada e timestamp flutuante na tela final (folha 0012), depois replicada em múltiplos tamanhos de dispositivo (iPhone e iPad, folha 0012, q0104), mostrando visualmente a afirmação final da fala de que o resultado "works across Apple devices".
