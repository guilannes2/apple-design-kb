# wwdc2026_314: Learn CSS Grid Lanes

Status visual: assistido: 8 de 8 folhas abertas, todos os códigos conferidos.

### wwdc2026_314 · folha 0001 · de 00:00 a 01:10 · código A7EBE
- Tela: abertura com card laranja "WWDC26" (q0001), o apresentador falando à câmera em estúdio (q0002, q0003), depois fundo cinza claro vazio (q0004) que evolui para diagramas de blocos coloridos tipo mosaico em grade 4 colunas (q0005, q0006), depois as mesmas formas em tom pastel claro e claro/rosa lado a lado (q0007), e por fim uma janela de navegador (barra de endereço "webkit.org") mostrando um mosaico de fotos reais de paisagem em blocos de tamanhos variados, tipo alvenaria (q0008, q0009).
- Construção de interface: mosaico com colunas de largura igual e alturas de bloco variáveis, blocos maiores ocupando duas linhas verticalmente, gap uniforme entre blocos. Nos quadros de fotos reais (q0008, q0009), janela de navegador com barra de título de app desktop (três botões coloridos, barra de endereço central, ícones de compartilhar e nova aba). Fotos preenchem cada célula do mosaico sem distorção aparente. Paleta do diagrama: azul médio sobre fundo cinza claro; segunda variante em dois tons pastel (azul claro e rosa claro) lado a lado, sugerindo comparação de dois layouts.
- Mudança entre quadros: de q0004 (tela vazia) para q0005 e q0006, surgem dois mosaicos de blocos azuis quase idênticos lado a lado, um à esquerda vazio e dois preenchidos à direita e centro. De q0006 para q0007, os blocos ficam esmaecidos em duas cores (azul claro à esquerda, rosa claro à direita), como comparação de variantes. De q0007 para q0008, os mesmos blocos recebem fotografias reais dentro da janela do navegador. De q0008 para q0009, o conteúdo do mosaico de fotos parece o mesmo, com pequena diferença de rolagem ou timing de carregamento.
- Com a fala: enquanto o apresentador se apresenta e descreve CSS Grid Lanes como "waterfall" e "brick wall" (mosaico tipo cascata), a tela mostra precisamente esse tipo de mosaico de blocos de alturas variadas, depois com fotos reais aplicadas.

### wwdc2026_314 · folha 0002 · de 01:13 a 02:37 · código A6365
- Tela: apresentador falando (q0010, q0011), depois diagramas de comparação entre "Flexbox" e "Grid": linhas horizontais com blocos de largura desigual dentro de faixas azuis com barras horizontais grossas (q0012 a q0014), seguido por diagrama "Gridbox" com grade pontilhada em 4 colunas e várias linhas horizontais finas (q0015), e finalmente diagramas "Grid" com grade de linhas azuis formando células, algumas células vazias em branco e uma foto de paisagem (campo florido ao pôr do sol) inserida numa célula, que cresce ocupando mais células em quadros seguintes (q0016 a q0018).
- Construção de interface: diagramas rotulados com texto no canto superior esquerdo ("Flexbox", "Gridbox", "Grid"). Blocos em Flexbox aparecem como faixas horizontais com segmentos de largura desigual separados por linhas brancas finas, empilhados em 4 ou 5 fileiras. O diagrama Grid mostra uma malha de linhas azuis (5 colunas por 4 linhas aproximadamente), com algumas células deixadas em branco (vazias) ao lado de células preenchidas em azul, ilustrando espaço não ocupado. Uma imagem real é inserida numa célula central e cresce horizontalmente ocupando duas células nos quadros seguintes.
- Mudança entre quadros: de q0013 para q0014, o rótulo muda de "Flexbox" para "Flexbox" novamente mas a disposição dos blocos internos muda (larguras diferentes), indicando outra direção de fluxo. De q0014 para q0015, o rótulo passa a "Gridbox" com estrutura pontilhada, mudança clara de tipo de diagrama. De q0016 para q0017, uma foto de paisagem aparece dentro de uma célula do grid antes vazia. De q0017 para q0018, essa mesma foto expande de uma célula para duas células horizontais, mostrando crescimento de tamanho.
- Com a fala: a fala explica que Flexbox dá um eixo e uma lane, e Grid dá duas dimensões com células que podem ficar com espaço vazio quando os itens têm proporções diferentes; a tela mostra exatamente essas células vazias no grid e depois a foto sendo esticada/inserida numa célula, ilustrando o problema de distorção ou espaço sobrando que a fala descreve.

### wwdc2026_314 · folha 0003 · de 02:49 a 03:58 · código 21267
- Tela: diagrama "Grid Lanes" com barras verticais laranja finas espalhadas sobre fundo cinza claro, representando colunas (q0019 a q0021, a última com o rótulo esmaecido em transição). Depois uma janela de navegador mostrando uma grade de caixas coloridas numeradas de 1 a 15 (cores variando entre laranja, amarelo, verde, azul), em colunas de alturas desiguais tipo mosaico (q0022). Segue apresentador falando (q0023, q0024, esta última vazia/transição), depois uma página de blog/notícias com colunas de texto e uma foto de mel (q0025), apresentador novamente (q0026), e um slide de título "Grid Lanes / Create a Grid Lanes container" com bloco de código CSS mostrando `.container { display: grid-lanes; }` (q0027).
- Construção de interface: barras verticais laranja finas indicam posições de colunas antes de qualquer conteúdo, como um guia estrutural. A grade de caixas numeradas 1 a 15 tem 4 colunas de largura igual e alturas de caixa variáveis, numeração sequencial esquerda para direita, topo para baixo, com cores diferenciando cada caixa. O slide de código usa tipografia monoespaçada, com "display: grid-lanes" destacado em azul claro (realce de sintaxe ou seleção), título "Grid Lanes" em negrito grande e subtítulo cinza menor.
- Mudança entre quadros: de q0019 para q0020, as barras verticais laranja mudam de posição e espaçamento, indicando reconfiguração de número de colunas. De q0020 para q0021, o diagrama inteiro esmaece (fade), sinalizando transição de cena. De q0026 para q0027, muda de vídeo da pessoa para slide de código.
- Com a fala: a fala introduz Grid Lanes como estrutura em uma dimensão (diferente de Grid, que usa duas). A tela mostra as colunas guia em laranja e depois a grade numerada de exemplo (provavelmente de um artigo/demo real do WebKit) demonstrando itens preenchendo colunas em ordem. O slide de código coincide com a fala descrevendo a primeira linha de CSS, "display: grid-lanes".

### wwdc2026_314 · folha 0004 · de 04:10 a 05:21 · código F2E5F
- Tela: série de slides "Grid Lanes" com bloco de código CSS à esquerda e diagrama de blocos azuis à direita, evoluindo por diferentes propriedades: "Create a Grid Lanes container" com `grid-template-columns: repeat(3, 1fr);` (q0028, q0029) mostrando 3 colunas de blocos azuis de alturas variadas; "Implement a brick variation" com `gap: 10px;` (q0031) e depois `grid-template-rows: repeat(3, 1fr);` (q0032, q0033) mostrando blocos dispostos horizontalmente em fileiras (brick wall, blocos fluindo da esquerda para direita); "Experiment with different layouts" com `grid-template-columns: 1fr 1fr 1fr;` (q0034) e depois `repeat(auto-fill, minmax(200px, 1fr))` com trecho de código destacado em azul claro (q0035, q0036) mostrando colunas de largura variável (uma coluna central mais larga).
- Construção de interface: layout padrão de slide técnico da Apple, com dois blocos lado a lado: painel de código à esquerda (fundo branco, fonte monoespaçada, sintaxe com cores: chaves e propriedades em preto, valores em azul) e painel visual à direita (blocos azuis sólidos em grade). Linha de código relevante à explicação atual aparece destacada com fundo azul claro (realce de leitura). Título do slide em negrito preto grande, subtítulo cinza abaixo.
- Mudança entre quadros: de q0028 para q0029, a linha de código destacada muda de `display: grid-lanes;` para `grid-template-columns: repeat(3, 1fr);`, e o diagrama de blocos à direita se redesenha mantendo 3 colunas mas com alturas de bloco diferentes. De q0031 para q0032, o rótulo do slide muda de "brick variation" para o mesmo título mas a propriedade destacada muda para `grid-template-rows`, e o diagrama de blocos muda de orientação vertical (colunas) para horizontal (fileiras), com blocos fluindo da esquerda para a direita em vez de cima para baixo. De q0032 para q0033, o mesmo diagrama horizontal se repete com leve variação de posição dos blocos. De q0034 para q0035, o código muda de `1fr 1fr 1fr` (colunas iguais) para `repeat(auto-fill, minmax(200px, 1fr))`, e o diagrama muda de 3 colunas iguais para uma coluna central visivelmente mais larga que as laterais. De q0035 para q0036, o número de colunas no diagrama aumenta e os blocos ficam mais estreitos.
- Com a fala: a fala explica que a unidade fr divide o espaço em frações iguais, depois mostra trocar `grid-template-columns` por `grid-template-rows` para virar "brick wall", e depois usar `auto-fill` com `minmax()` para deixar o navegador decidir o número de colunas; a tela acompanha exatamente essas trocas de propriedade linha a linha, com o diagrama de blocos mudando de orientação e proporção a cada mudança de código.
### wwdc2026_314 · folha 0005 · de 05:24 a 06:22 · código D7D16
- Tela: sequência de slides "Grid Lanes / Experiment with different layouts" com código à esquerda usando `repeat(auto-fill, minmax(8rem, 1fr) minmax(14rem, 2fr))` e diagrama de blocos azuis à direita com uma coluna mais estreita em azul claro (q0037, q0038, q0039 com apresentador). Depois "Grid Lanes / Control individual items" (q0040 a q0045): diagramas de 3 colunas de blocos azuis com um retângulo vazio contornado em laranja se movendo e crescendo, código mostrando `.item { grid-column: span 2; }` e depois `grid-column: 2 / span 2;`.
- Construção de interface: retângulo de contorno laranja (sem preenchimento) usado como indicador de seleção ou item ativo sobre a grade de blocos azuis, mudando de posição e de tamanho (de uma célula para abranger duas células, span 2) entre os quadros. Bloco de código mostra propriedade `.item` com `grid-column` destacada em azul claro.
- Mudança entre quadros: de q0037 para q0038, o trecho de código destacado muda de `grid-template-columns: repeat(auto-fill, minmax(8rem,1fr)...)` completo para a mesma linha com leve diferença de seleção; o diagrama de blocos permanece com uma coluna clara à direita mais estreita. De q0040 para q0041, aparece um retângulo vazio de contorno laranja substituindo um bloco azul na grade, indicando item selecionado antes de virar código. De q0041 para q0042, o retângulo laranja cresce de uma célula para uma área maior (spanning), e os blocos ao redor se reorganizam para preencher o espaço restante. De q0043 para q0044 para q0045, o retângulo de contorno laranja aparece em posições diferentes na grade (topo, depois no meio), sempre ocupando o dobro da largura de uma célula normal, acompanhando a propriedade `grid-column: 2 / span 2` no código.
- Com a fala: a fala descreve dar mais espaço a um item colorido usando `grid-column: span 2` e depois posicionar exatamente com `grid-column: 2 / span 2`. A tela mostra esse item como um retângulo vazio de contorno laranja crescendo e se movendo dentro da grade de blocos, coerente com a explicação verbal.

### wwdc2026_314 · folha 0006 · de 06:34 a 08:02 · código FB00B
- Tela: slides "Grid Lanes / Integrate subgrid" (q0046 a q0048) mostrando um card com contorno laranja contendo uma miniatura de imagem cinza e linhas de texto simulado, inserido dentro da grade de blocos azuis; código evolui de `.card { display: grid-lanes; grid-template-columns: subgrid; grid-column: span 2; }` para `.card { display: grid; grid-template-columns: subgrid; grid-column: span 2; }`. Depois apresentador (q0049). Depois slides "Flow tolerance / Improve item positioning" (q0050 a q0054) com pequenos blocos numerados 1, 2, 3, 4 em azul (e um item mais escuro, roxo) organizados em duas colunas, alguns com contornos verdes vazios abaixo indicando posição alternativa.
- Construção de interface: o "card" dentro da grade tem contorno laranja distinto dos demais blocos azuis sólidos, e internamente contém dois elementos simulados (um ícone de imagem em cinza claro com moldura, e linhas horizontais representando texto), digno de nota como subgrid aninhado. Nos slides de flow tolerance, blocos numerados de tamanho uniforme, cor azul (1,2) e roxo/azul-escuro (3,4) alternando, com contornos verdes vazios usados para indicar posições possíveis ou zona de tolerância abaixo dos itens 1 e 2.
- Mudança entre quadros: de q0046 para q0047, a linha de código destacada muda de `display: grid-lanes; grid-template-columns: subgrid;` para a mesma linha com `grid-template-columns: subgrid;` sozinha destacada, e o card de contorno laranja permanece na mesma posição na grade. De q0047 para q0048, a propriedade `display` muda de `grid-lanes` para `grid` no código, e o card na grade muda de posição, agora ocupando as duas primeiras colunas no topo em vez de estar mais abaixo. De q0050 para q0051, dois contornos verdes vazios aparecem abaixo dos blocos 1 e 2, e uma linha horizontal laranja aparece entre os blocos superiores e essas áreas vazias, indicando um limiar ou linha de corte. De q0052 para q0053, os blocos numerados se reorganizam de uma fileira horizontal (1,2) para duas fileiras (1,2 em cima e 4,3 embaixo, com 4 mais escuro). De q0053 para q0054, os blocos 1, 2 e 3 ficam esmaecidos (tom claro) enquanto o bloco 4 permanece escuro, destacando um item específico.
- Com a fala: a fala explica subgrid, onde o card de recipe (imagem + texto) passa a participar do layout da grade pai via `display: grid-lanes` e depois `grid-template-columns: subgrid`, e a tela mostra exatamente esse card com miniatura de imagem e linhas de texto dentro de um contorno destacado. Em seguida, a fala descreve flow tolerance como o mecanismo que decide em qual coluna cada novo item entra, e a tela mostra blocos numerados se reorganizando entre duas colunas com indicadores verdes e linha de corte laranja.

### wwdc2026_314 · folha 0007 · de 08:14 a 09:33 · código 8C757
- Tela: continuação dos diagramas "Flow tolerance" com blocos numerados 1, 2, 3 (q0055, q0056, com linha horizontal laranja indicando limiar) e depois 1, 2, 3, 4 em grade 2x2 completa (q0057). Segue apresentador falando (q0058, q0059), um quadro em branco/transição (q0060), depois uma captura de tela de ferramenta de desenvolvedor (DevTools/Web Inspector) mostrando uma grade de imagens com rótulos de medida tipo "minmax(14rem, 1fr)" sobre cada coluna, painel de código-fonte HTML à esquerda embaixo e painel de propriedades CSS à direita com opções de overlay de grade (checkboxes: "Line Numbers", "Line Names", "Area Names", "Extended Grid Lines", "Grid Overlays") (q0061), e mais dois quadros do apresentador (q0062, q0063).
- Construção de interface: no DevTools (q0061), overlay colorido sobre a grade real do navegador mostra números de linha, rótulos de tamanho de coluna (minmax) sobrepostos no topo de cada coluna, e um menu lateral com checkboxes para controlar quais elementos do overlay de grade são exibidos, réplica de uma ferramenta real de inspeção de layout. Painel de código-fonte abaixo lista elementos `<figure>` repetidos.
- Mudança entre quadros: de q0055 para q0056, um terceiro bloco numerado (3) aparece abaixo do bloco 1, junto com uma linha horizontal laranja indicando o limiar de tolerância entre as colunas. De q0056 para q0057, um quarto bloco (4) aparece completando uma grade 2x2 cheia, sem mais a linha laranja, indicando a distribuição final entre as duas colunas segundo a regra de flow tolerance. De q0059 para q0060, a tela vai de vídeo do apresentador para um quadro inteiramente em branco, uma transição.
- Com a fala: a fala descreve o teste que o navegador faz para decidir se um item entra na coluna mais alta ou na mais curta, dependendo do "flow-tolerance" (padrão 1em), e recomenda abrir o Web Inspector, que tem suporte completo a Grid Lanes com linhas de coluna/linha, números de ordem e visualização de gaps; a captura de tela do DevTools na folha mostra exatamente esse overlay de inspeção sobre uma grade real de imagens.

### wwdc2026_314 · folha 0008 · de 09:39 a 10:24 · código 86726
- Tela: três slides de encerramento "Next steps" sobre uma mesa de madeira com uma caixa verde de ferramentas, um iPhone azul e um caderno verde ao fundo, com lista de itens de texto que cresce a cada quadro: primeiro sem itens (q0064), depois "Explore the Grid Lanes Field Guide" e "Try it out on your own web content" (q0065), depois acrescido de "Share your feedback" (q0066). Segue o apresentador sorrindo à câmera para encerrar (q0067, q0068, este último com iluminação mais quente/alaranjada, possível transição de créditos finais).
- Construção de interface: slide de encerramento com fundo fotográfico realista (mesa de madeira, objetos físicos estilizados representando "ferramentas" e "notas"), texto em lista simples preto sobre fundo claro, título "Next steps" em negrito no canto superior esquerdo. Sem grades ou componentes de UI nesta folha, apenas o slide de fechamento.
- Mudança entre quadros: de q0064 para q0065, aparecem duas linhas de texto na lista que antes estava vazia. De q0065 para q0066, uma terceira linha ("Share your feedback") é adicionada à lista. De q0067 para q0068, a cor de fundo do vídeo do apresentador muda de tom neutro/cinza para um tom alaranjado quente, sugerindo troca de cena final.
- Com a fala: a fala fecha o vídeo convidando a explorar o "Grid Lanes Field Guide", testar em projetos próprios e compartilhar feedback, e a lista de texto no slide reproduz exatamente esses três itens conforme são mencionados.

## Síntese visual (folhas 0001 a 0008)

- Comparação visual entre modos de layout: a folha 0002 mostra diagramas rotulados lado a lado ("Flexbox", "Gridbox", "Grid") com a mesma paleta de blocos azuis, deixando explícito, só pela imagem, que células vazias em branco aparecem no diagrama Grid quando os itens têm alturas diferentes, reforçando visualmente o problema que a fala descreve em palavras.
- Padrão de slide técnico: da folha 0003 em diante, todo conceito novo é apresentado no mesmo template, código CSS monoespaçado à esquerda com a linha relevante destacada em fundo azul claro, e diagrama de blocos azuis sólidos à direita reagindo à mudança de código. Esse padrão se repete idêntico por sete conceitos diferentes (container, brick variation, layouts diferentes, controle de item, subgrid, flow tolerance), o que a fala não descreve, é a imagem que revela essa disciplina de repetição de template.
- Uso de contorno laranja como marcador de "item em foco": aparece em vários papéis diferentes ao longo das folhas 0005 e 0006, ora marcando um item vazio sendo redimensionado (folha 0005), ora marcando um card de subgrid completo com conteúdo (folha 0006), sempre com a mesma cor de destaque, criando uma linguagem visual consistente de "isto é o que estamos editando agora".
- Indicadores verdes de posição alternativa: na folha 0006 (flow tolerance), contornos verdes vazios abaixo dos blocos numerados marcam onde um item poderia ir, e uma linha horizontal laranja marca o limiar de decisão; nenhuma dessas cores foi mencionada na fala, é um código de cor só visível na imagem (laranja para limiar/foco, verde para posição alternativa).
- Ferramenta real de inspeção: a folha 0007 mostra uma captura de tela genuína do Web Inspector com o painel de configuração do overlay de grade (checkboxes para números de linha, nomes de linha, nomes de área, linhas de grade estendidas), algo que a fala apenas resume como "full support" sem detalhar as opções, mas a imagem lista os controles exatos disponíveis.
- Progressão pedagógica por acréscimo de propriedades: nas folhas 0004 e 0005, cada slide novo mantém o bloco de código anterior e apenas acrescenta ou troca uma propriedade (de `display: grid-lanes` para `grid-template-columns`, depois `gap`, depois trocando para `grid-template-rows`), com o diagrama à direita se redesenhando a cada mudança; a imagem evidencia que o ensino é construído por incrementos visíveis de código, um conceito por vez.
